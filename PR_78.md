# feat(ai-data): 이미지 및 스캔 PDF CLOVA OCR 파이프라인 구축

## 관련 이슈

- Closes #78
- 상위 이슈: #76
- 선행 이슈: #77

## 작업 배경

#77에서 일반 PDF 55건의 PDF.js 텍스트 추출을 완료했습니다. 이번 PR은 텍스트 레이어가 없는 이미지 첨부파일과 PDF.js만으로 처리할 수 없는 PDF 페이지를 NAVER Cloud CLOVA OCR General API V2로 추출·저장하는 후속 작업입니다.

초기에는 Tesseract 5 CLI를 검토했지만 실제 한국어 홍보 포스터에서 영역 탐지, 읽기 순서와 한글 인식 품질이 충분하지 않아 CLOVA OCR로 전환했습니다. 이미지와 PDF 모두 동일한 입력 검증·전처리·OCR 결과 정규화 기반을 사용하며, 호출량·상태 전이·복구·임시 파일 정리를 명시적으로 관리합니다.

## 주요 변경 사항

### 1. CLOVA OCR 클라이언트와 응답 파서

- General OCR V2 multipart 요청 구현
- `X-OCR-SECRET` 인증과 file-backed Blob 사용
- 원본 URL·파일명·attachment ID를 외부 요청에서 제외
- timeout 및 response stream 크기 제한
- HTTP·네트워크·schema 오류를 안전한 내부 코드로 변환
- 429·5xx·네트워크 오류만 제한적으로 재시도
- timeout·인증·입력·schema 오류는 자동 재시도하지 않음
- `lineBreak` 우선, bounding box 좌표 fallback 읽기 순서
- field 수, 평균 confidence, 빈 텍스트, API 호출·재시도 횟수 집계
- Secret, Invoke URL, OCR 본문과 전체 임시 경로를 로그에서 제외

### 2. 이미지 입력 검증과 최소 전처리

- JPEG·PNG signature 및 Sharp metadata 검증
- 이미지 크기, 픽셀 수와 예상 decode memory 제한
- EXIF orientation 반영
- 투명 PNG를 흰색 배경으로 합성
- 제한 초과 시에만 축소
- 원본 파일을 수정하지 않는 임시 PNG 처리
- 성공·실패 시 임시 파일과 job 디렉터리 cleanup

Tesseract 전용 grayscale, normalize, traineddata 검사와 CLI adapter는 제거했습니다. Poppler에서도 사용하는 공통 subprocess runner는 유지했습니다.

### 3. IMAGE OCR plan·dry-run·write CLI

```bash
npm run extract:program-attachments -- --type IMAGE --limit <N> --plan
npm run extract:program-attachments -- --type IMAGE --limit <N> --dry-run
npm run extract:program-attachments -- --type IMAGE --limit <N>
```

- plan은 다운로드·OCR·DB 변경 없이 대상과 예상 호출량만 계산
- dry-run은 실제 다운로드·OCR 후 DB 불변 확인
- write는 조건부 claim으로 `PENDING → PROCESSING → COMPLETED/FAILED` 전이
- attempt, 실패 코드, 추출 시각과 OCR 결과를 원자적으로 저장
- 파일별 실패를 격리하여 다음 대상 계속 처리
- 배치 limit, 실제 호출 수, retry, empty 결과와 cleanup 집계

### 4. checksum 기반 OCR 결과 재사용

- 다운로드한 파일의 SHA-256으로 완료된 IMAGE OCR donor 조회
- 동일 checksum·파일 형식의 저장 결과를 API 호출 없이 재사용
- 서로 다른 본문을 가진 donor가 있으면 conflict로 처리하고 실제 OCR 수행
- 재사용 결과도 독립적인 상태 전이와 attempt 기록 유지
- 중복 분석·실패 진단·혼합 배치 검증 스크립트 추가

실제 제한 배치에서 중복 이미지 결과 재사용과 API 호출 절감이 정상 동작했습니다. IMAGE 156건은 모두 `COMPLETED`이며 PENDING·PROCESSING·FAILED가 남지 않았습니다.

### 5. MIXED PDF 페이지 분석과 렌더링

PDF.js 페이지를 다음과 같이 분류합니다.

```text
TEXT
LOW_DENSITY
OCR_CANDIDATE
```

- 텍스트 페이지와 OCR 후보가 함께 있으면 `MIXED`
- OCR 후보 페이지 번호의 범위·중복·오름차순 검증
- Poppler `pdftocairo`를 argument 배열로 실행
- 페이지별 PNG signature, 크기와 metadata 검증
- timeout, 출력 크기, 페이지 수와 작업 디렉터리 containment 제한
- PDF.js 비후보 페이지를 보존하고 OCR 후보만 교체
- `[Page N]` marker를 포함해 페이지 순서대로 병합

### 6. MIXED PDF plan·render·OCR dry-run·write

```bash
npm run extract:program-attachments -- --type PDF_OCR --mixed-only --limit 1 --plan
npm run extract:program-attachments -- --type PDF_OCR --mixed-only --limit 1 --render-dry-run
npm run extract:program-attachments -- --type PDF_OCR --mixed-only --limit 1 --ocr-dry-run
npm run extract:program-attachments -- --type PDF_OCR --mixed-only --limit 1 --write
```

- 실행 모드는 반드시 하나만 지정
- MIXED write는 limit 1만 허용
- renderer와 CLOVA preflight 후에만 대상 조회·처리
- write 범위에서는 CLOVA retry를 0으로 강제
- `COMPLETED + PDFJS_TEXT_PARTIAL` snapshot이 일치할 때만 claim
- 완료 update도 claim 직후 상태와 시각이 일치할 때만 실행
- 실패 시 기존 PDF.js 본문·metadata·extractor snapshot을 조건부 복원
- 완료 결과는 `PDFJS_TEXT_OCR_MERGED`로 저장

### 7. stale PROCESSING 복구

```bash
npm run recover:attachment-extractions -- \
  --type PDF_OCR \
  --mixed-only \
  --stale-after-minutes 60 \
  --limit 20 \
  --plan
```

- 활성 MIXED PDF의 오래된 PROCESSING만 선택
- 본문·extractor·failure·시각 snapshot 조건 검증
- plan/apply 명시적 분리
- 경쟁 update는 강제 덮어쓰기 없이 skip
- attempt, lastAttemptedAt, 기존 본문과 metadata 보존
- 다운로드·렌더·CLOVA 호출 없음

실제 DB의 stale PROCESSING 대상은 0건이어서 apply는 실행하지 않았습니다.

### 8. OCR_REQUIRED 스캔 PDF 처리 기반

```bash
npm run extract:program-attachments -- \
  --type PDF_OCR \
  --ocr-required-only \
  --limit 5 \
  --plan
```

- `FAILED + failureCode = OCR_REQUIRED`인 활성 PDF만 선택
- write limit 1~5, retry 0
- PDF signature와 페이지 수 검증
- 최대 50페이지를 순차 렌더·OCR
- 페이지별 결과를 `[Page N]` 순서로 병합
- 성공 시 `CLOVA_OCR_PDF`로 저장
- 중간 페이지 실패 시 다음 페이지를 처리하지 않고 FAILED 전이
- claim·completion 경쟁에서 다른 worker 결과를 덮어쓰지 않음

현재 실제 DB에는 OCR_REQUIRED 대상이 없어 read-only plan만 실행했습니다. 운영 OCR_REQUIRED write는 실행하지 않았습니다.

## 실제 처리 결과

### IMAGE

| 항목 | 결과 |
|---|---:|
| 전체 IMAGE | 156 |
| COMPLETED | 156 |
| PENDING / PROCESSING / FAILED | 0 / 0 / 0 |
| JPEG / PNG | 125 / 31 |
| checksum conflict | 0 |
| 임시 파일 잔여 | 0 |

이미지 처리는 단건 검증 후 5건·20건 제한 배치로 확대했으며, checksum 재사용 혼합 배치와 최종 잔여 배치를 거쳐 완료했습니다.

### MIXED PDF 실제 저장

전체 Mock·fixture 회귀와 read-only preflight를 통과한 후 MIXED PDF 1건을 정확히 한 번 write했습니다.

| 항목 | 결과 |
|---|---:|
| selected / claimed / completed | 1 / 1 / 1 |
| failed / skipped | 0 / 0 |
| 전체 / PDF.js / OCR 페이지 | 4 / 3 / 1 |
| render attempted / succeeded | 1 / 1 |
| CLOVA API calls / retries | 1 / 0 |
| OCR field 수 | 10 |
| 평균 confidence | 0.7798277339999998 |
| OCR 빈 결과 | false |
| 병합 raw / cleaned 문자 수 | 2,576 / 2,437 |
| page marker / 중복 / 누락 | 4 / 0 / 0 |
| 비후보 raw / cleaned 보존 | true / true |
| 품질 경고 | 0 |
| 복원 실행 | 0 |
| 임시 파일 잔여 | 0 |

저장 직후 동일 MIXED plan은 `selected = 0`, stale PROCESSING plan과 OCR_REQUIRED plan도 각각 0을 반환했습니다. 추가 CLOVA 호출이나 DB 변경은 없었습니다.

### 최종 DB 집계

| 항목 | 결과 |
|---|---:|
| ProgramCase / Session | 349 / 20 |
| Attachment 전체 / 활성 | 237 / 237 |
| IMAGE COMPLETED | 156 |
| PDF COMPLETED | 55 |
| PDFJS_TEXT | 54 |
| PDFJS_TEXT_OCR_MERGED | 1 |
| HWP/HWPX | 26 |
| logical key 중복 | 0 |
| 고아 attachment | 0 |
| PROCESSING | 0 |

## 테스트

추가·확장한 주요 테스트:

- CLOVA 설정, multipart 요청과 응답 schema
- timeout·응답 크기·HTTP 오류·재시도 정책
- 이미지 metadata·전처리·cleanup
- IMAGE plan·dry-run·write 상태 전이
- 다건 실패 격리와 checksum 결과 재사용
- Poppler version·timeout·exit·signature·크기 제한
- PDF OCR 후보 검증과 페이지 병합
- MIXED render/OCR dry-run DB 불변
- MIXED claim·completion 경쟁과 snapshot 복원
- OCR_REQUIRED 3페이지 성공
- 2페이지 실패 시 후속 페이지 미호출
- renderer 실패 시 OCR 호출 0
- 빈 OCR 정상 결과
- 페이지 제한 초과
- claim·completion 경쟁과 cleanup
- stale PROCESSING plan/apply와 동시성

최종 검증 명령:

```bash
npx prisma validate
npx prisma generate
npm run build
npm run test:attachment-extraction
npm run test:attachment-preservation
npm run test:attachment-regression
npm run test:attachment-ocr
npm run test:pdf-ocr-foundation
npm run test:pdf-ocr-write
npm run test:attachment-recovery
```

모든 검증이 통과했으며 전체 데이터 회귀 테스트에서 349/20/237가 보존됐습니다.

## 보안 및 운영 안전성

- 실제 `.env`와 `.local`은 Git ignore
- Secret, Invoke URL, attachment ID, 원본 URL과 checksum 값 미포함
- OCR·PDF·병합 본문과 원본 파일 미포함
- 외부 요청에 원본 URL과 원본 파일명 미전송
- SSRF 방어와 다운로드 크기·timeout 제한 유지
- renderer는 shell 문자열이 아닌 argument 배열로 실행
- 모든 실제 배치는 limit과 예상 호출량을 plan으로 먼저 확인
- MIXED 실제 write는 CLOVA 최대 1회·retry 0으로 실행
- 실제 OCR_REQUIRED write는 대상 부재로 미실행
- EC2 배포, PM2 재시작, migration 변경 없음

## 주요 변경 파일

### 설정·클라이언트

- `apps/backend/src/config/attachmentOcr.ts`
- `apps/backend/src/config/clovaOcr.ts`
- `apps/backend/src/services/attachment/clovaOcrClient.ts`
- `apps/backend/src/services/attachment/clovaOcrResponseParser.ts`
- `apps/backend/src/services/attachment/ocrEngine.ts`

### 이미지 OCR

- `apps/backend/src/services/attachment/imageMetadata.ts`
- `apps/backend/src/services/attachment/imagePreprocessor.ts`
- `apps/backend/src/services/attachment/imageOcrProcessor.ts`
- `apps/backend/src/services/attachment/imageOcrDryRunService.ts`
- `apps/backend/src/services/attachment/imageOcrReuse.ts`
- `apps/backend/src/services/attachment/imageOcrFailureDiagnostic.ts`

### PDF OCR

- `apps/backend/src/services/attachment/pdfPageRenderer.ts`
- `apps/backend/src/services/attachment/pdfOcrMerger.ts`
- `apps/backend/src/services/attachment/pdfOcrPlanService.ts`
- `apps/backend/src/services/attachment/pdfOcrRequiredService.ts`
- `apps/backend/src/services/attachment/attachmentRecoveryService.ts`
- `apps/backend/src/cli/recoverAttachmentExtractions.ts`

### CLI·테스트·문서

- `apps/backend/src/cli/extractProgramAttachments.ts`
- `apps/backend/scripts/test-attachment-ocr.js`
- `apps/backend/scripts/test-image-ocr-cli.js`
- `apps/backend/scripts/test-pdf-ocr-foundation.js`
- `apps/backend/scripts/test-pdf-ocr-write.js`
- `apps/backend/scripts/test-attachment-recovery.js`
- `docs/ai-data/IMAGE_PDF_OCR_IMPLEMENTATION_DESIGN.md`

## 제외 범위

- 실제 OCR_REQUIRED PDF write
- HWP/HWPX 본문 추출
- OCR 결과의 검색·추천·RAG 연결
- OCR confidence 기반 자동 폐기 또는 사람 검수 UI
- 운영 EC2 배포와 PM2 재시작
- Vercel 및 frontend 변경
- Prisma schema 및 migration 변경

## 리뷰 참고 사항

- IMAGE write는 기존 CLI 호환성을 위해 실행 모드 플래그 없이 동작하지만 PDF OCR write는 `--write`를 반드시 요구합니다.
- MIXED PDF는 전체 문서를 OCR하지 않고 PDF.js로 읽히지 않는 후보 페이지만 처리합니다.
- OCR_REQUIRED는 모든 페이지를 순차 처리하므로 실제 대상 발생 시 반드시 plan과 작은 limit부터 시작해야 합니다.
- timeout은 서버 측 처리 완료 여부를 알 수 없어 과금 중복 방지를 위해 자동 재시도하지 않습니다.
- OCR confidence는 관측값으로 저장·보고할 뿐 낮다는 이유로 텍스트를 자동 삭제하지 않습니다.
- `apps/frontend/next-env.d.ts`의 로컬 생성 변경은 이 PR 범위가 아니며 포함하지 않습니다.

## 체크리스트

- [x] CLOVA OCR 클라이언트·응답 파서
- [x] Secret-safe 설정과 오류 처리
- [x] IMAGE OCR plan·dry-run·write
- [x] IMAGE 156건 처리 완료
- [x] checksum 기반 결과 재사용
- [x] Poppler PDF 페이지 렌더링
- [x] MIXED PDF 후보 OCR·병합
- [x] MIXED PDF 실제 저장 1건
- [x] stale PROCESSING 복구 기반
- [x] OCR_REQUIRED 다중 페이지 처리 기반
- [x] Mock·fixture·전체 데이터 회귀 테스트
- [x] 실제 DB 무결성 및 cleanup 확인
- [x] Secret·URL·ID·checksum·본문 미포함
- [x] 설계·운영 결과 문서 업데이트

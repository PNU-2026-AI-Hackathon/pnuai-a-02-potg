# 전체 정제 1~5단계 실행 결과

이 문서는 `ISSUE_PROGRAM_FULL_NORMALIZATION.md`의 1단계(전체 인벤토리)부터 351건 전체 배치와 상태별 분류까지의 결과다.

브랜치: `feat/program-full-normalization` (커밋하지 않은 작업 트리 상태)

## 0. 최종 결과 요약 (351건 전체)

| 상태 | 건수 | 의미 |
| --- | ---: | --- |
| `AUTO_REVIEW_CANDIDATE` | 57건 | 규칙이 명확히 적용됨. 사람이 원본과 대조할 수 있는 상태 |
| `MANUAL_REVIEW_REQUIRED` | 27건 | 값 충돌 또는 회차 미추출로 사람 확인 필요 |
| `SINGLE_SESSION_EVENT` | 17건 | 하루짜리 행사라 회차가 없는 것이 정상 |
| `OCR_REQUIRED` | 250건 | OCR 정책 확정 전까지 대기 |
| `EXTRACTION_FAILED` | **0건** | 다운로드·형식 판별 실패 없음 |

| 항목 | 결과 |
| --- | ---: |
| 회차가 구조화된 프로그램 | 49건 |
| 구조화된 전체 회차 | 395개 |
| 문서 경로 다운로드 성공률 | 100% (83/83) |
| 사람이 확인해야 하는 레코드 | 27건 (7.7%) |
| 본문만으로 게시 가능한 레코드 | 227건 |
| OCR 큐 고유 파일 | 244개 (250 레코드) |
| 대표 20건 회귀 | 차이 0건 |
| OCR·LLM API 호출 | 0건 |

사용자 결정으로 반영한 사항은 아래 7절에 정리했다.

## 1. 전체 351건의 실제 구성

이슈 초안의 추정치를 실제 수치로 대체한다.

```powershell
cd apps/backend
npm run program-attachment-inventory:build -- --profile all
```

| 프로파일 | 초안 추정 | 실제 | 비고 |
| --- | ---: | ---: | --- |
| `text_only` | 약 17건 | **18건** | #140의 정제 대상 17건과 1건 차이 |
| `text_with_supplement` | 약 198건 | **198건** | 일치 |
| `image_only` | 미확정 | **69건** | 본문 없음, 본문 이미지 보유 |
| `attachment_only` | 미확정 | **66건** | 본문 없음, 첨부만 보유 |
| `empty` | 미확정 | **0건** | 해당 없음 |

실첨부 확장자 분포는 `.hwp` 26, `.pdf` 57, `.jpg` 125, `.png` 31이다. **`.hwpx`는 한 건도 없다.** 이슈 초안에 넣어둔 `HWPX_TEXT` 경로는 이번 데이터에서는 사용되지 않는다.

### 초안 대비 가장 큰 차이

레코드 단위로 실제 처리 경로를 판정하면 다음과 같다.

| 처리 경로 | 건수 | 내용 |
| --- | ---: | --- |
| `DOC_EXTRACT` | **83건** | HWP/PDF 첨부가 있어 현재 규칙으로 추출 가능 |
| `TEXT_ONLY` | 18건 | 첨부 없음. 본문 정제 결과만 사용 |
| `TEXT_WITH_IMAGE` | 126건 | 본문은 있으나 첨부·이미지는 OCR 없이 열 수 없음 |
| `NO_TEXT_IMAGE_ONLY` | 124건 | 본문이 없고 이미지뿐이라 OCR 없이는 내용이 없음 |

**즉 현재 규칙으로 첨부 내용을 실제로 추출할 수 있는 대상은 351건 중 83건(23.6%)이다.** 나머지 250건은 OCR 정책이 확정되어야 진행할 수 있고, 그중 124건은 본문조차 없어 OCR 없이는 게시할 내용이 사실상 없다.

이슈 초안은 `text_with_supplement` 198건 전체가 첨부 추출 대상인 것처럼 범위를 잡았으나, 실제로는 198건 중 72건만 HWP/PDF를 갖고 있고 126건은 이미지뿐이다.

OCR 큐 대상 250건이 참조하는 이미지 파일은 **고유 244개**다. 같은 포스터를 여러 회차 레코드가 공유하는 경우가 있어 파일 단위로 접으면 호출량이 소폭 줄어든다.

## 2. 구현한 것

| 파일 | 내용 |
| --- | --- |
| `src/cli/buildProgramAttachmentInventory.ts` | `--profile all` 옵션 추가. 기본값과 기존 산출물은 그대로 유지 |
| `src/cli/runProgramAttachmentBatch.ts` | 신규. 경로별 배치 실행과 4개 상태 분류 |
| `src/cli/reportProgramAttachmentBatch.ts` | 신규. 상태별 산출물과 통계 생성 |
| `scripts/test-program-attachment-batch.js` | 신규. 경로 판정·상태 해석·재개 동일성 검증 |
| `src/cli/buildProgramAttachmentEnrichmentSamples.ts` | `processSample` 등을 배치에서 재사용하도록 export만 추가 |
| `src/services/.../mergeProgramAttachment.ts` | `combinedBasicInfo` export만 추가 |

배치는 대표 20건과 **동일한 `processSample` 코드 경로**를 사용한다. 별도 구현을 두면 회귀 비교가 성립하지 않기 때문이다.

실행 명령:

```powershell
cd apps/backend
npm run program-attachment-inventory:build -- --profile all
npm run program-attachment-batch:run -- --route HWP_TEXT
npm run program-attachment-batch:run -- --route PDF_CLASSIFY --limit 30
npm run program-attachment-batch:run -- --route TEXT_ONLY
npm run program-attachment-batch:run -- --route IMAGE_OCR
npm run program-attachment-batch:report
```

## 3. 전체 실행 결과 (351/351건)

문서 경로 83건(HWP 26 + PDF 57)과 첨부를 열지 않는 경로 268건을 모두 처리했다.

| 항목 | 결과 |
| --- | ---: |
| 처리 완료 | **351건** |
| `AUTO_REVIEW_CANDIDATE` | 57건 |
| `MANUAL_REVIEW_REQUIRED` | 27건 |
| `SINGLE_SESSION_EVENT` | 17건 |
| `OCR_REQUIRED` | 250건 |
| `EXTRACTION_FAILED` | **0건** |
| 문서 경로 다운로드 성공률 | 100% (83/83) |
| 회차가 구조화된 프로그램 | 49건 |
| 구조화된 전체 회차 | 395개 |
| 사람이 확인해야 하는 레코드 | 27건 |

다운로드 실패와 형식 판별 실패가 0건이다. 대체 URL 탐색 로직을 쓸 일도 없었다.

수동 검수 27건의 사유별 구성은 다음과 같다. 한 레코드가 여러 사유를 가질 수 있다.

| 사유 | 건수 | 성격 |
| --- | ---: | --- |
| `CURRICULUM_NOT_EXTRACTED` | 17 | 회차를 못 읽음. 원본 표 확인 필요 |
| `ATTACHMENT_TARGET_CONFLICT` | 18 | 기본정보와 첨부의 대상 값이 다름 |
| `ATTACHMENT_INSTRUCTOR_CONFLICT` | 4 | 강사 값이 다름 |
| `HWP_EMBEDDED_CONTENT_REVIEW` | 4 | HWP 임베디드 이미지 원본 대조 필요 |
| `ATTACHMENT_TITLE_CONFLICT` | 2 | 제목 표기가 다름 |

값 충돌은 대표 20건에서 확정한 "덮어쓰지 않고 검수로 보낸다" 규칙이 그대로 동작한 결과다.

### 대표 20건 회귀

배치 경로로 실행한 결과를 `merged-samples.json`과 비교했다.

- 20건 전부 포함, **회차 수·상태·선택 페이지가 모두 동일**
- 차이 0건

기존 테스트도 모두 통과한다.

```text
test:program-board-data                  PASS
test:program-attachment-inventory        PASS
test:program-attachment-section-matcher  PASS
test:program-attachment-merge            PASS
test:program-attachment-batch            PASS
frontend  npx tsc --noEmit               PASS
```

백엔드 `tsc`에는 기존 오류 47건이 있으나 전부 `src/routes/me.ts`·`posts.ts`의 Prisma 스키마 불일치로, 이 작업 이전부터 있던 것이다. 이번 변경 파일에는 타입 오류가 없다.

## 4. 대표 20건 밖에서 규칙이 무너지는 지점

문서 경로 56건 중 **27건이 회차를 하나도 만들지 못했다.** 전부 대표 20건 밖의 신규 레코드다. 원인은 네 가지로 나뉜다.

### (A) 회차 셀이 `1회차` 형식이면 표 전체를 버린다 — 실제 결함 → **수정 완료**

> `stripSessionUnit()`을 추가해 회차 칸의 `회차`·`차시`·`회기`·`주차` 단위를 벗긴 뒤 판정하도록 고쳤다.
> `2808`·`2809`·`2810`이 각 4회차로 복구되어 회차 추출 성공이 29건 → 32건, 전체 회차가 259개 → 271개가 됐다.
> 대표 20건과 기존 테스트는 모두 그대로 통과한다. 아래는 수정 전 진단 기록이다.


`documentCurriculumExtractor.ts:65`

```ts
if (!/^\d{1,2}$/.test(sessionText)) continue;
```

회차 칸의 텍스트가 정확히 `1`, `2` 같은 숫자여야 한다. `1회차`처럼 단위가 붙으면 모든 행이 걸러져 표가 통째로 사라진다.

원본 PDF를 직접 열어 확인한 결과다.

- `2808 생각톡톡! 메이커 교실`: 페이지 1에 `차시 | 세부 교육내용 | 담당강사 | 교수방법` 표와 `1회차`~`4회차` 이상이 명확히 존재. 추출 결과 0행
- `2810 책과 함께하는 하브루타`: 페이지 3에 동일 구조, `1회차 오리엔테이션 -하브루타 이해와 라포형성`. 추출 결과 0행
- 같은 계열 문서인 대표 20건의 `2700`은 8행 정상 추출

즉 구간 매칭(score 0.95)과 라벨 추출(목표·재료비·교육기간)은 성공했는데 회차 표만 비었다. 이 문서군에는 대표 20건에 없던 **`담당강사` 열**도 있다.

해당 레코드: `2808`, `2809`, `2810` (+ `2709`는 값 충돌로 이미 수동 검수 대상)

### (B) 경고 없이 조용히 비는 문제 — 분류 설계의 구멍

(A)의 레코드들은 `extractionWarnings`가 비어 있어 `AUTO_REVIEW_CANDIDATE`로 분류됐다. 검수자가 `auto-review.json`을 열면 목표·재료비는 채워져 있고 회차만 없는 화면을 보게 되는데, **회차가 원래 없는 프로그램인지 못 읽은 것인지 구분할 단서가 없다.**

현재 "회차 0건 + 경고 없음 + 자동 검토 후보"는 24건이다. 이 중 상당수는 아래 (C)(D)처럼 정상이지만, 지금 구조로는 정상과 결함이 섞여 있다.

### (C) 단일 회차 이벤트를 회차 프로그램과 같은 실패로 취급한다 — 분류 문제

`2736`~`2747` 12건은 제목이 `풍선아트 체험 13:00 ~ 13:20(20분)` 형태다. 20분 단위 체험 슬롯을 레코드로 쪼갠 것이라 회차 표가 애초에 없다. 제목에 시간 범위가 들어 있어 구간 매칭도 `NOT_FOUND`(score 0)가 된다.

`3302 영화 속 역사 이야기`, `3634`·`3635` 작가와의 만남, `3636` 콘서트 4건도 단일 행사라 회차가 없는 것이 정상이다.

이들은 결함이 아니지만 지금은 "회차 0건"으로만 남아 (A)와 구분되지 않는다.

### (D) HWP 구간 매칭 실패 6건 — 원인 미확인

`2522`, `2600`, `2604`, `2605`, `2606`, `3938`이 `NOT_FOUND`(score 0)다. HWP 텍스트는 받았으나 제목 매칭에 실패했다. 원인은 아직 확인하지 않았다.

### 정리 (전체 실행 후 최종)

| 구분 | 처리 |
| --- | --- |
| (A) `N회차` 표기로 표 유실 | 수정 완료. 회차 추출 성공 49건 |
| (B) 구분이 안 되던 상태 | `CURRICULUM_NOT_EXTRACTED` 경고로 17건이 검수로 분리됨 |
| (C) 단일 행사라 회차 없음 | `SINGLE_SESSION_EVENT` 17건으로 분류 |
| (D) HWP 구간 매칭 실패 | 검수 대상에 포함. **원인은 아직 미확인** |

(D) HWP 매칭 실패 6건의 원인은 8절에 정리했다. 원인이 세 가지로 나뉘며, 그중 하나는 **다른 레코드의 데이터 오염**을 동반한다.

## 8. HWP 구간 매칭 실패 6건의 원인

원본 HWP를 직접 추출해 확인한 결과 원인이 세 가지다.

### 원인 A — 공유 HWP에 구간 분리가 없다 (4건 실패 + 1건 오염)

`강의계획서(22. 겨울방학).hwp` 한 파일에 5개 프로그램이 들어 있다. PDF는 페이지 단위로 구간을 나누지만 **HWP는 페이지 개념이 없어 문서 전체를 1페이지로 넘기고 `singleProgramDocument: true`로 처리한다.** 매칭은 앞 600자만 채점하므로 문서 맨 앞에 있는 프로그램 하나만 일치하고 나머지는 전부 `NOT_FOUND`가 된다.

| sourceId | 제목 | 첨부 내 위치 | 결과 |
| --- | --- | ---: | --- |
| `2607` | 명화와 놀자 | 문서 맨 앞 | `WHOLE_DOCUMENT` score 1 |
| `2606` | 영어 동화 속 나만의 스토리텔링 | 618자 | `NOT_FOUND` |
| `2605` | 문학과 놀아보는 글쓰기 수업 | 1090자 | `NOT_FOUND` |
| `2604` | 동화에 퐁당! 미술에 풍덩! | 1497자 | `NOT_FOUND` |
| `2600` | 창의력 쑥쑥 보드게임 | 2438자 | `NOT_FOUND` |

**더 큰 문제는 성공한 것처럼 보이는 `2607`이다.** 문서 전체를 자기 구간으로 가져가서:

- 기본정보상 `[4회]` 프로그램인데 **24회차**가 추출됐다. 같은 4회차가 6번 반복된 형태다.
- 다른 프로그램들의 대상·강사를 흡수해 `ATTACHMENT_TARGET_CONFLICT` 6건, `ATTACHMENT_INSTRUCTOR_CONFLICT` 4건이 발생했다. `초등 1~2학년`, `유아 7세`, `초등 5~6학년`, 강사 `전주현`·`조영인`·`신동민` 등 이 프로그램과 무관한 값들이다.

즉 이 충돌 경고들은 "값이 다르다"가 아니라 **"문서에 여러 프로그램이 들어 있다"는 신호**였다. `2607`은 `MANUAL_REVIEW_REQUIRED`로 분류되어 사람이 보게 되므로 잘못된 채로 게시되지는 않는다.

첨부 URL은 레코드마다 달라서 URL만으로는 공유 문서를 탐지할 수 없다. 파일명 또는 체크섬으로 판단해야 한다.

### 원인 B — 첨부 본문에 제목 표기가 없다 (1건)

`2522 청소년을 위한 독서 처방전`의 첨부는 685자짜리 단일 프로그램 문서인데, 본문이 `강의계획서 / 1. 강의 개요 …`로 시작하고 **프로그램명을 적은 줄이 아예 없다.** 파일명(`청소년을 위한 독서 처방전.hwp`)에만 제목이 있다.

제목이 없으니 제목 매칭 실패는 정상 동작이다. 파일명을 매칭 근거로 추가하면 해결된다.

### 원인 C — 제목 표기 불일치와 `강의명` 라벨 미인식 (1건)

`3938`은 두 가지가 겹쳤다.

| | 값 |
| --- | --- |
| 게시글 제목 | `2025년 들락날락 영어랑 놀자 2기(초등반)` |
| 첨부 표기 | `강의명 \| 들락날락 영어랑 놀자 ( Group B 초등반 )` |

- `headingCandidates()`의 라벨 정규식이 `프로그램명`과 `강좌명`만 인식한다. **`강의명`은 없어서** 제목 후보 자체를 못 만든다.
- 토큰 커버리지도 `2025년`, `2기`가 첨부에 없어 0.75에 못 미친다.

### 정리

| 원인 | 영향 | 해결 방향 |
| --- | --- | --- |
| A. 공유 HWP 구간 분리 없음 | 4건 실패 + 1건 회차·대상·강사 오염 | HWP 텍스트를 `프로그램명`/`강좌명`/`강의명` 머리말 기준으로 구간 분할 |
| B. 첨부에 제목 없음 | 1건 | 파일명을 매칭 근거로 추가 |
| C. `강의명` 미인식 + 표기 불일치 | 1건 | 라벨 정규식에 `강의명` 추가, 연도·기수 토큰 가중치 완화 |

### 수정 결과 — 세 원인 모두 반영

| 수정 | 내용 |
| --- | --- |
| A | `splitHwpProgramSections()` 추가. `프로그램명`·`강좌명`·`강의명` 머리말이 2개 이상이면 구간으로 나누고, 각 구간을 페이지처럼 매칭한다. 머리말이 하나면 기존처럼 단일 문서로 처리한다. 공유 문서에서는 보조 HWP 파서와 임베디드 이미지 추출을 쓰지 않는다. 파일 전체를 대상으로 하므로 어느 구간의 값인지 확정할 수 없기 때문이다. |
| B | `fileNameMatchesTitle()` 추가. 단일 프로그램 문서에서 본문 매칭이 실패하면 첨부 파일명이 게시글 제목과 맞는지로 판단한다. |
| C | `headingCandidates()`와 `startsProgramSection()`에 `강의명` 추가. 제목 토큰에서 `2025년`·`2기` 같은 연도·기수 표기를 제외하되, 빼고 남는 토큰이 2개 미만이면 프로그램을 구분할 수 없으므로 그대로 둔다. |

| sourceId | 이전 | 이후 |
| --- | --- | --- |
| `2600` | `NOT_FOUND` 0회차 | `SECTION_MATCHED` **4회차** |
| `2604` | `NOT_FOUND` 0회차 | `SECTION_MATCHED` **4회차** |
| `2605` | `NOT_FOUND` 0회차 | `SECTION_MATCHED` **4회차** |
| `2606` | `NOT_FOUND` 0회차 | `SECTION_MATCHED` **4회차** |
| `2522` | `NOT_FOUND` 0회차 | `WHOLE_DOCUMENT` **2회차** (파일명 근거) |
| `3938` | `NOT_FOUND` 0회차 | `WHOLE_DOCUMENT` 0회차 (매칭은 성공, 회차 표는 미추출 → 검수 대상 유지) |
| `2607` | `WHOLE_DOCUMENT` **24회차 (오염)** | `SECTION_MATCHED` **4회차 (정상)** |

전체 지표 변화는 다음과 같다. 회차 총계가 2 줄어든 것은 `2607`의 허위 24회차가 정상 4회차로 교정되며 20개가 빠지고, 복구된 5건에서 18개가 늘어난 결과다.

| 항목 | 수정 전 | 수정 후 |
| --- | ---: | ---: |
| `AUTO_REVIEW_CANDIDATE` | 57 | 59 |
| `MANUAL_REVIEW_REQUIRED` | 27 | 25 |
| 회차가 구조화된 프로그램 | 49 | **54** |
| 구조화된 전체 회차 | 395 | 393 |
| 값 충돌 | 25 | **19** |

대표 20건은 회차 160개, 자동 17건·수동 3건 기준을 그대로 유지한다.

### 새로 드러난 문제 — HWP 표의 `담당강사` 열 (2건) → 9절에서 해결

`2523`, `2600`은 HWP 표에 `차시 | 세부 교육내용 | 담당강사 | 교수방법` 구조가 있는데, 텍스트 기반 구조화가 `담당강사`를 활동 내용 뒤에 붙이고 `교수방법`을 `비고`로 넣었다. 아래 9절의 표 셀 구조 전환으로 해결됐다.

## 9. 검수 피드백 반영 — HWP를 표 셀 구조로 읽도록 전환

검수에서 확인된 네 가지 문제의 원인이 모두 하나였다. **HWP를 평탄화된 텍스트로 읽고 있었다는 점이다.**

PDF에는 이미 `평탄화된 텍스트만으로 열 경계를 추측하지 않고 원본 표 셀 구조를 우선 사용한다`는 규칙이 있었지만, HWP는 페이지 개념이 없다는 이유로 텍스트 경로에 남아 있었다. 확인해 보니 `kordoc.parse()`는 HWP도 표 IR로 돌려주며 셀 안의 줄바꿈과 `<도서명>` 표기까지 보존한다.

### 평탄화 텍스트에서 잃던 것

| 항목 | 평탄화 텍스트 | 표 셀 구조 |
| --- | --- | --- |
| 셀 안 줄바꿈 | 공백으로 합쳐짐 | `살바도르 달리-기억의 지속\n초현실주의 화가…` 보존 |
| `<도서명>` | 기본 파서가 제거 (`<` `>` 0개) | 보존 (`<한 눈에 반한 우리 문화 20>`) |
| 열 경계 | 위치로 추측 → `담당강사`가 활동에 붙음 | 머리글 기준 정확 분리 |
| 표 아래 `※` 주의사항 | 마지막 회차의 비고로 붙음 | 별도 행으로 분리 |
| 다음 프로그램 머리말 | 앞 구간 끝에 섞임 | 표 블록 단위로 분리 |

### 반영한 규칙

1. **HWP도 표 셀 구조를 우선 사용한다.** `extractHwpProgramSections()`가 표 블록마다 프로그램 구간을 만들고, 각 구간의 회차·라벨·주의사항을 셀에서 직접 읽는다. 표 파싱이 실패하면 기존 텍스트 분할로 물러선다.
2. **활동 내용의 줄바꿈을 보존한다.** 다른 칸은 한 줄로 정리하되 활동 내용만 `cleanMultiline()`으로 줄 구조를 유지한다. 화면은 이미 `white-space: pre-line`이라 그대로 표시된다.
3. **회차 칸이 비고 내용만 있는 행은 앞 회차가 이어지는 줄로 합친다.** `2480 일본어 초급반`처럼 한 회차가 두 행에 걸친 표가 있다.
4. **표 아래 `※` 행은 회차 비고가 아니라 이용 안내로 보낸다.** 회차 번호가 없고 `※`로 시작하는 행이 대상이며, 한 셀에 여러 줄이면 줄 단위로 나눈다.
5. **번호 붙은 자유문 구획을 프로그램 내용으로 읽는다.** `1. 강의 개요`, `2. 강의 운영 방법`처럼 표가 아니라 문단으로 적은 계획서가 있다.
6. **줄표만 적힌 재료비·교재비는 `없음`으로 표시한다.** 기존 규칙에 `[-ㅡ—]`가 있었으나 비교 함수가 기호를 먼저 지워서 실제로는 동작하지 않았다. 원문 값에서 직접 판정하도록 고쳤다. 본문에 다른 값이 있으면 기존 충돌 규칙이 검수로 보낸다.
7. **문서 머리말은 어느 구간에도 넣지 않는다.** `금정구 작은도서관 / 2022년 겨울방학 프로그램 강의계획서`가 앞 프로그램의 마지막 회차에 섞이던 문제를 없앤다.
8. **표 IR의 라벨은 공백을 지운 형태로 오므로 텍스트 경로의 라벨과 같은 항목으로 합친다.** `학습자준비물`과 `학습자 준비물`이 따로 게시되던 중복을 없앤다.

### 검수 지적별 결과

| 지적 | 결과 |
| --- | --- |
| `2489` 표 아래 `※` 두 줄이 8차시 비고에 들어감 | 8차시 비고 `null`, 두 줄 모두 `이용 안내 > 운영 안내`로 이동 |
| `2522` `1. 강의 개요`·`2. 강의 운영 방법`이 누락 | 프로그램 내용에 두 항목 모두 반영 |
| `2600` 활동 내용의 줄바꿈이 사라짐 | `쉽고 간단한 보드게임\n누구나 쉽게하는…\n(사용게임 : 벼룩서커스, 마헤)`로 보존 |
| `2605` 재료비·교재비 `-` | 둘 다 `없음` |
| `2605` `<한 눈에 반한 우리 문화 20>` 누락 | 복원. 글머리표도 줄바꿈으로 표시 |
| `2605` 4회차 비고에 다음 계획서 제목 | 제거됨 (`null`) |
| `2523`·`2600` 활동 내용에 강사명 | 전 레코드 0건. `교수방법`이 자기 열로 들어감 |

### 전체 지표 변화

| 항목 | 텍스트 경로 | 표 셀 구조 |
| --- | ---: | ---: |
| `AUTO_REVIEW_CANDIDATE` | 59 | 60 |
| `MANUAL_REVIEW_REQUIRED` | 25 | **24** |
| 회차가 구조화된 프로그램 | 54 | **55** |
| 구조화된 전체 회차 | 393 | **401** |
| 값 충돌 | 19 | **18** |
| `ATTACHMENT_INSTRUCTOR_CONFLICT` | 4 | **0** |
| `CURRICULUM_NOT_EXTRACTED` | 17 | **11** |
| 활동 내용에 강사명 섞임 | 2 | **0** |
| 줄바꿈이 보존된 프로그램 | 0 | **47 / 55** |

대표 20건은 회차 160개, 자동 17건·수동 3건 기준을 그대로 유지한다. 위 규칙 중 줄바꿈 보존, `<도서명>` 보존, 담당강사 분리, `※` 분리, 다음 계획서 제목 제거는 `test:program-attachment-merge`에 회귀 검사로 고정했다.

## 5. 남은 결정

0. **OCR 정책**: 2026-08-15 확정. `ISSUE_PROGRAM_OCR_POLICY.md` 참고. 엔진은 CLOVA 유지, 호출 상한 300회, 최소 신뢰도 0.8, 미달 시 검수, OCR 결과는 전량 사람 검수.
1. **`HWPX_TEXT` 경로 유지 여부**: 실데이터에 `.hwpx`가 0건이다. 이슈에서 빼도 된다.
2. **`담당강사` 열 처리**: HWP 2건과 PDF 일부에서 강사 열이 활동 내용에 섞인다. 위 8절 참고.
3. **`3938` 회차 표**: 매칭은 성공했으나 16주 일정표를 회차로 읽지 못한다. 원인 미확인.

## 5-1. 검수 화면 연결

`/programs/attachment-review`가 대표 20건이 아니라 **배치 결과 351건 전체**를 읽는다.

- 데이터 원본: `program-attachment-batch/full.json`. 배치를 돌리지 않은 환경에서는 기존 대표 20건 산출물로 물러선다.
- 첨부 추출문은 게시 데이터에 넣지 않는 원칙에 따라 `program-attachment-batch/evidence.json`으로 분리해 저장하고, 검수 화면만 이를 읽는다.
- 상태별 필터를 제공하며 기본 화면은 `수동 검수 필요`다. 각 카드에 사유 코드(`CURRICULUM_NOT_EXTRACTED`, `ATTACHMENT_TARGET_CONFLICT` 등)를 표시한다.
- 첨부가 없는 레코드(OCR 대기·텍스트 전용)도 화면이 깨지지 않는다. 신뢰도·선택 페이지 대신 본문 게시 가능 여부와 OCR 대상 파일을 보여준다.
- HWP는 페이지가 아니라 구간이므로 `N번째 구간 선택`으로 표기한다.

## 6. 하지 않은 것

- 커밋, 푸시, PR 생성
- OCR·LLM API 호출 (0건)
- 게시판 UI 변경
- OCR 큐 250건의 실제 처리 (정책 확정 후 별도 이슈)

## 7. 사용자 결정으로 반영한 사항

### (1) `1회차` 표기 정규화 — 반영

`stripSessionUnit()`으로 `회차`·`차시`·`회기`·`주차` 단위를 벗긴 뒤 판정한다. 회차 추출 성공이 29건 → 32건(부분 실행 기준)으로 늘었고, 전체 실행에서는 83건 중 49건이 회차를 갖는다.

### (2) `CURRICULUM_NOT_EXTRACTED` 경고 — 반영

회차가 0건이면서 하루짜리 행사가 아닌 경우 경고를 남기고 `MANUAL_REVIEW_REQUIRED`로 보낸다. 경고 메시지에 구간 매칭 성공 여부를 함께 적어 "구간은 찾았는데 표만 못 읽음"과 "구간 자체를 못 찾음"을 구분한다.

이 규칙으로 17건이 새로 검수 대상이 됐다. 이전에는 경고 없이 자동 검토 후보로 통과하던 건들이다.

- `2708`, `2709`: 값 충돌로만 잡히던 건이 회차 미추출 사유까지 함께 기록됨
- `3302 영화 속 역사 이야기`: 2개월짜리인데 회차 0건 → 검수로 이동
- `2522`, `3938`: 본문이 `N회차`를 언급하는데 회차 0건 → 검수로 이동

### (3) `SINGLE_SESSION_EVENT` 상태 — 반영

판정 근거는 **교육 시작일과 종료일이 같은 하루짜리**이며, 본문·첨부가 `N회차`를 언급하면 하루로 보여도 단일 행사로 보지 않는다. 근거가 약하면 자동 분류하지 않고 검수로 보낸다.

원본이 시간대별로 쪼개져 등록된 경우 **쪼개진 그대로 각각의 레코드로 유지한다.** 프로그램 단위로 합치지 않는다.

17건이 분류됐다.

- `2736`~`2747` 풍선아트 체험 12건 (20분 단위 시간대별 레코드를 그대로 보존)
- `2834`, `2835` 레진아트 열쇠고리 1·2차
- `3634`, `3635` 작가와의 만남 2건
- `3636` 클래식 콘서트

2개월간 운영되는 `3302`는 하루짜리가 아니므로 단일 행사로 분류되지 않고 검수 대상으로 갔다.

### (4) 본문 게시 가능 표시 — 반영

`bodyPublishable` 필드를 추가했다. 본문이 있는 227건(텍스트 전용 18 + 본문+이미지 126 + 문서 경로 83)은 본문만으로 화면을 구성할 수 있다.

**다만 이것은 정제 완료가 아니다.** 본문+이미지 126건은 `bodyPublishable: true`이면서 동시에 `reviewStatus: OCR_REQUIRED`, `attachmentReviewStatus: ATTACHMENT_UNCHECKED`를 유지한다. 회차 같은 상세 정보가 포스터 이미지 안에 있으므로 OCR로 보완해야 최종 완료가 된다. 이 조건은 테스트로 고정했다.

OCR 큐는 본문 유무로 나뉜다.

| 구분 | 건수 | 상태 |
| --- | ---: | --- |
| 본문 있음 + 이미지 첨부 | 126건 | 본문 기준 게시 가능, OCR로 회차 보완 필요 |
| 본문 없음 + 이미지뿐 | 124건 | OCR 전까지 게시할 내용 없음 |

고유 파일은 244개다. 같은 포스터를 최대 11개 레코드가 공유하는 경우가 있어 파일 단위로 호출하면 중복을 줄일 수 있다.

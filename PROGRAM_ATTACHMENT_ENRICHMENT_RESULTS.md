# 프로그램 첨부 구간 선택·구조화 결과

## 수행 범위

HWP 10건과 텍스트 PDF 10건을 대표 표본으로 선정해 다운로드, 문서 추출, 프로그램 구간 선택, 표 구조화, 본문 병합을 검증했다. 이미지 OCR이나 유료 API는 사용하지 않았다.

## 실행 방법

```powershell
cd apps/backend
npm run test:program-attachment-section-matcher
npm run program-attachment-enrichment:samples -- --per-type 10
npm run program-attachment-merge:samples
```

결과는 `.local/program-attachment-enrichment/samples.json`과 `merged-samples.json`에 생성된다.

## 결과

| 항목 | 결과 |
| --- | ---: |
| 대표 프로그램 | 20건 |
| 프로그램 구간 선택 성공 | 20건 |
| 회차 구조화 프로그램 | 20건 |
| 구조화된 회차 | 160개 |
| 자동 검토 후보 | 17건 |
| 값 충돌로 수동 검수 필요 | 3건 |

`자동 검토 후보`는 자동 게시 승인이 아니라 사람이 원본과 비교할 수 있는 상태를 의미한다.

## 확정 규칙

- 단일 HWP는 제목 일치 후 전체 문서를 선택한다.
- 공유 PDF는 제목을 정규화해 프로그램별 페이지를 선택한다.
- 공백·기호 차이와 `창의력up` 같은 중간 문구 삽입은 핵심 제목 단어로 보완한다.
- 동일 파일명의 기본 URL이 실패하면 검증 가능한 정상 대체 URL을 탐색한다.
- PDF 평문보다 표 셀 구조를 우선하며 프로그램명, 목표, 소개를 분리한다.
- `차시(날짜)` 같은 결합 머리글과 다음 페이지로 이어지는 표를 처리한다.
- 병합 셀의 분류는 해당 회차 범위에 상속하고 세부 활동은 각 행에서 추출한다.
- 교수방법·준비물·비고는 독립 필드로 보존한다.
- 표 내부 이미지 수와 회차 수가 일치하고 순서가 확인될 때만 회차에 연결한다.
- HWP 보조 파서와 임베디드 이미지 추출은 기본 파서가 놓친 제목·도서 표지를 보완한다.

## 대표적으로 복원한 예외

- `알콩달콩 책놀이 세상`: 도형에 있던 회차 제목 복원
- `마음과 만나는 그림책 테라피`: 참고도서명과 표지 8개 연결
- `어린이 과학탐구교실`: 결합 머리글, 8회차, 이미지, 변동 안내 복원
- `I Love story`: 대체 URL, 목표, 8회차 책 표지, 전체 참고자료 복원
- `영어로 배우는 사회과학`: 병합된 Society·Policy·Math·Science와 일자별 도서명·표지 분리
- `생각톡톡! 미술아 놀자`: 2페이지 연속 표, 강의목표 경계, 준비물과 이미지 복원

## 알려진 한계와 후속 작업

- 대표 20건에서 확정한 규칙을 전체 351건에 아직 적용하지 않았다.
- 이미지 수와 회차 수가 다르거나 셀 연결을 증명할 수 없는 문서는 자동 배치하지 않는다.
- 스캔 PDF와 포스터 이미지는 별도 OCR 정책이 필요하다.
- 전체 변환 후 수동 검수 대상의 규모를 산출하고 최종 게시판 UI를 조정한다.

# 프로그램 첨부 모듈 2단계 검증 결과

## 검증 범위

1단계에서 만든 `text_with_supplement` 198건 인벤토리에서 유형별 실제 금정구 첨부 5개씩, 총 15개를 검증했다.

- HWP 5개
- PDF 5개
- 이미지 5개
- 외부 유료 OCR API 호출 0회

검증 명령:

```powershell
cd apps/backend
npm run program-attachment-samples:validate -- --per-type 5
```

상세 결과는 `.local/program-attachment-validation/report.json`에 생성된다. 다운로드한 원본과 전처리 파일은 검증 후 삭제한다.

## 결과 요약

| 유형 | 표본 | 성공 | 확인한 기능 |
| --- | ---: | ---: | --- |
| HWP | 5 | 5 | 다운로드, 실제 형식 판별, 본문·HTML 표 추출 |
| PDF | 5 | 5 | 다운로드, 실제 형식 판별, 페이지별 텍스트 추출과 문서 분류 |
| 이미지 | 5 | 5 | 다운로드, 실제 형식 판별, 크기·픽셀 안전성 검사, OCR 입력 전처리 |

HWP 표본에서는 프로그램 개요뿐 아니라 차시, 일자, 세부 교육내용, 준비물 등이 표 형태로 추출됐다. 따라서 기존 HWP 추출기는 다음 단계의 회차별 활동 구조화에 재사용할 수 있다.

PDF 표본 5개는 모두 텍스트 레이어가 있는 `TEXT` 유형이었다. 17페이지에서 약 11,000자의 텍스트를 추출했으며 회차별 교육내용도 포함되어 있었다.

이미지 표본은 JPEG와 PNG 모두 형식 및 메타데이터 검사와 OCR용 PNG 전처리에 성공했다. OCR 엔진 호출은 비용 정책이 정해지지 않았으므로 실행하지 않았다.

## 발견한 중요 사항

### 하나의 첨부가 여러 프로그램을 포함할 수 있음

PDF 표본 5건은 URL이 각각 존재하지만 내려받은 파일의 SHA-256이 모두 같았다. 파일명은 `강의계획서(상반기).pdf`이며 여러 프로그램의 계획서를 묶은 17페이지 통합 문서다.

따라서 첨부 전체 텍스트를 각 프로그램에 그대로 붙이면 다른 프로그램 내용까지 섞인다. 다음 단계에서는 프로그램 제목·대상·도서관 등의 단서를 이용해 현재 프로그램에 해당하는 페이지나 구간만 선택해야 한다.

### 동일 포스터가 여러 회차에서 공유됨

이미지 표본에서도 서로 다른 회차가 같은 파일 체크섬을 공유했다. OCR 결과를 체크섬 기준으로 재사용하는 기존 기능은 비용 절감에 유효하다. 단, 추출된 포스터 내용 중 현재 회차와 관련된 범위를 선택하는 작업은 별도로 필요하다.

## 아직 통과하지 않은 부분

- 현재 실행 환경에 `pdftocairo`가 없어 PDF 페이지 이미지 변환은 실행되지 않았다.
- 외부 OCR API는 호출하지 않아 실제 한글 인식 품질은 아직 확인하지 않았다.
- HWPX 실제 표본은 이번 198건 표본에서 선택되지 않았다.
- 기존 `test-image-ocr-cli.js`는 현재 소스가 아니라 오래된 `dist` 산출물을 읽어 assertion 한 건이 실패한다. 이미지 전처리 실표본 검증은 성공했지만, 전체 백엔드 빌드/`dist` 동기화 문제는 별도로 해결해야 한다.

## 다음 단계에 미치는 영향

첨부 추출 결과를 본문과 병합하기 전에 다음 중간 단계가 필요하다.

1. 체크섬으로 동일 파일을 묶어 중복 다운로드와 OCR을 방지한다.
2. 통합 PDF/HWP에서 현재 프로그램에 해당하는 구간을 찾는다.
3. 선택한 구간에서 회차별 활동, 교재, 준비물 등 신규 정보만 구조화한다.
4. 본문과 충돌하거나 프로그램 구간을 특정하지 못하면 사람 검수로 보낸다.


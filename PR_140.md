## 작업 개요

금정구 공공예약 서비스에서 크롤링한 작은도서관 프로그램을 MOIRA 프로그램 게시판에 표시할 수 있도록 본문 구조 추출, 규칙 기반 정제, 텍스트형 17건 데이터 생성과 검수 UI를 구현했습니다.

Closes #140

## 작업 내용

- [x] 원사이트 상세 페이지를 기본정보·프로그램 내용·안내사항으로 분리
- [x] HTML 교육계획표의 행·열·병합 셀과 표 내부 이미지 보존
- [x] 대상·기간·장소·신청·문의 등 라벨 변형을 사전 기반으로 정규화
- [x] 모집인원 충돌 시 명확한 본문 인원을 우선하고 모호한 경우 검수 상태로 분류
- [x] 기본정보와 본문의 중복 항목 및 반복 제목 제거
- [x] 안내사항을 장소·비용·신청·운영·개인정보·문의 주제로 분류
- [x] 반복 회차 프로그램을 시리즈로 연결
- [x] 텍스트형 프로그램 17건을 생성하는 `program-board/v1` 파이프라인 구현
- [x] 기존 게시판 디자인을 따르는 목록·상세 UI와 원사이트 신청 링크 구현
- [x] 표 셀 이미지 배열이 없는 과거 크롤링 데이터에서도 상세 페이지가 열리도록 호환 처리
- [x] 정제 규칙과 향후 첨부파일 처리 방향 문서화

## 구현 범위

### 포함

- 전체 재크롤 351건 분석
- 본문 텍스트 또는 HTML 표가 있고 이미지·첨부파일이 없는 텍스트형 17건
- 프로그램 목록 및 상세 페이지 프로토타입
- 사전 커버리지 보고서와 데이터 단계 중복 회귀 테스트

### 제외

- 이미지 포스터 OCR
- HWP/HWPX 강의계획서 내용 추출·정제
- PDF 텍스트·표 추출 및 이미지 PDF OCR
- DB/Prisma 저장 및 운영 배포
- 공공예약 사이트를 대신하는 MOIRA 내부 수강신청

## 사용자 영향

- 대상, 강사, 모집인원, 기간, 시간, 접수 상태와 비용을 한눈에 볼 수 있습니다.
- 본문 자유문은 프로그램 소개·운영 정보·신청 안내·문의로 구분됩니다.
- HTML 교육계획표는 평문으로 무너지지 않고 표 구조로 표시됩니다.
- 중복 정보는 제거하되 상세 시간·주소처럼 추가 가치가 있는 정보는 보존합니다.
- 신청하기 버튼은 해당 공공예약 원사이트를 엽니다.

## 검증 결과

- [x] 정제 모듈 격리 실행 성공
- [x] 최신 351건 입력에서 텍스트형 17건 생성 성공
- [x] 텍스트형 17건 제목·소개·구조화 값 중복 회귀 테스트 통과
- [x] 라벨 사전: 964개 중 949개 매핑, 15개 의도적 무시, 미분류 0
- [x] 프런트엔드 TypeScript 검사 통과
- [x] 프로그램 목록 및 17개 상세 페이지 HTTP 200 확인
- [x] 대표 20건 원사이트 비교 20/20 일치
- [x] 기존 첨부 모듈의 다운로드·파일 판별·PDF 텍스트·HWP 텍스트·PDF OCR 계획 단위 테스트 통과

### 알려진 제한

- 전체 백엔드 `npm run build`는 기존 Prisma Client와 현재 스키마 사이의 타입 불일치로 실패합니다. 프로그램 정제 모듈은 `ts-node` 격리 실행으로 검증했습니다.
- 도서관 사전은 351건 중 1건(`성인/구서sk뷰 1단지`)이 미해결입니다.
- 이미지·HWP·PDF 내용은 이번 PR에서 구조화하지 않습니다.
- 첨부 모듈 자체의 안전한 mock 테스트는 통과했지만, 금정구 실제 첨부를 현재 게시판 정제 파이프라인에 연결하는 작업은 후속 범위입니다.

## 테스트 방법

```powershell
cd apps/backend
node -r ts-node/register src/cli/buildProgramBoardData.ts --input .local/geumjeong-small-library-crawl/geumjeong-small-library-programs-2026-08-12T11-53-21-300Z.json --out .local/program-board --profile text-only
$env:PROGRAM_BOARD_CRAWL='.local/geumjeong-small-library-crawl/geumjeong-small-library-programs-2026-08-12T11-53-21-300Z.json'
$env:PROGRAM_BOARD_MODULE_ROOT='src'
node -r ts-node/register scripts/test-program-board-data.js
```

```powershell
cd apps/frontend
npx tsc --noEmit
npm run dev
```

- 목록: `http://127.0.0.1:3000/programs`
- 자유문 예시: `http://127.0.0.1:3000/programs/3052`
- HTML 표 예시: `http://127.0.0.1:3000/programs/4354`

## 스크린샷

> 아래 자리에 GitHub PR 편집 화면에서 스크린샷을 드래그해 추가해 주세요.

### 프로그램 목록

<!-- 프로그램 목록(/programs) 스크린샷 -->

_스크린샷 추가 예정_

### 자유문 상세 화면

<!-- 2월 벙개 독서회(/programs/3052) 스크린샷 -->

_스크린샷 추가 예정_

### HTML 교육계획표 상세 화면

<!-- 똑똑한 지구인 프로젝트(/programs/4354) 스크린샷 -->

_스크린샷 추가 예정_

## 후속 작업 제안

1. 본문과 보조자료가 함께 있는 레코드를 `TEXT_SUFFICIENT`, `TEXT_PARTIAL`, `SUPPLEMENT_REQUIRED`로 판정
2. 기존 HWP 추출 모듈을 소수 샘플에서 재검증한 뒤 강의계획서 정제에 연결
3. 텍스트 PDF와 이미지 PDF를 분리해 처리
4. 포스터 이미지 OCR은 비용·정확도·사람 검수 절차를 확정한 후 진행

## 관련 문서

- `PROGRAM_DATA_NORMALIZATION_RULES.md`

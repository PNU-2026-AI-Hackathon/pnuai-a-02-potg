# PR: feat(ai-data): ProgramCase SearchProfile 파일럿 및 검색 MVP 구축

Closes #107

## 목적

ProgramCase 단위 SearchProfile의 분포 분석, 결정적 생성, 대표 30건 KURE-v1 임베딩, 파일 기반 검색 및 기존 Chunk 검색과의 비교 UI가 기술적으로 실행 가능한지 검증합니다.

이 PR은 SearchProfile이 기존 검색보다 우수하다는 것을 입증하지 않습니다. 실제 검색 품질은 동일 후보군과 사람 relevance 판정이 준비된 후속 이슈에서 평가합니다.

## 주요 변경

- ProgramCase 349건 read-only 분포 분석 및 공개 안전 통계 JSON 생성
- 최소 taxonomy와 결정적 규칙 기반 SearchProfile 생성기 추가
- 희소 taxonomy·정보 의존성을 반영한 대표 30건 선정
- 대표 검색 문서와 KURE-v1 1024차원 로컬 embedding 30건 생성
- 파일 기반 SearchProfile cosine 검색과 기존 Chunk P0 비교 API/UI 추가
- Session 회차 수를 `COUNT(DISTINCT Session.id)`로 정확히 집계
- Session 관계가 없을 때 1회로 처리하는 동작을 파일럿 fallback으로 명시
- 자동 결과를 검색 품질 평가가 아닌 `weak rule-consistency diagnostic`으로 정정
- operation/session 조건이 필요한 질의는 자동 진단에서 `INDETERMINATE` 처리
- 사람이 확인한 부자연스러운 검색 사례와 평가 한계 문서화

## 파일럿 범위

```text
Chunk 검색 후보군: 349 ProgramCase / 888 chunks
SearchProfile 후보군: 대표 30 ProgramCase
```

후보군이 서로 다르므로 양쪽 결과를 직접적인 성능 비교로 해석할 수 없습니다.

## 규칙 일치 기반 진단

재실행 결과:

```text
SAME_SIGNAL_RANK: 3
PROFILE_SIGNAL_AHEAD: 4
INDETERMINATE: 3
```

이 값은 동일하거나 유사한 taxonomy 규칙에서 신호가 발견된 순위를 나타낼 뿐, 사람 relevance 기반의 개선·동등·악화 판정이 아닙니다.

Chunk Top 5 총 50개 중 ATTACHMENT 30개도 관찰값일 뿐 편향 감소의 증거가 아닙니다.

## 관찰된 실패 사례

- `성인 대상 디지털 교육`에서 한자 프로그램이 높은 순위에 나타남
- `여러 회차로 진행되는 글쓰기 수업`에서 영어 프로그램이 상위에 나타남
- `지역 주민이 참여하는 공동체 활동`에서 수학·과학 보드게임이 상위에 나타남
- `과학 체험과 만들기`에서 비과학 프로그램이 상위에 포함됨

## 검증

- Backend TypeScript build 통과
- Frontend production build 통과
- Frontend lint 통과
- Python 관련 단위 테스트 4개 통과
- read-only SearchProfile generate 통과
- 규칙 일치 기반 진단 10개 질의 재실행
- Session row count와 기존 DISTINCT activity count 차이: 현재 데이터 0건
- generate 전후 프로필 의미 내용 동일
- 대표 프로필 30/30 유지
- 기존 로컬 embedding 파일 hash 동일
- Prisma schema/migration 변경 없음
- 운영 DB write 없음
- 기존 DB embedding 888건 변경 없음

## 결론과 후속 작업

이번 파일럿은 SearchProfile 파이프라인의 기술적 실행 가능성을 확인했습니다. 검색 품질은 아직 검증되지 않았습니다.

후속 이슈에서는 다음을 별도로 수행합니다.

- 349건의 동일 검색 corpus
- lexical·dense·hybrid 기준선
- 여러 run의 Top K pooling
- 사람의 0~3 relevance 판정과 qrels
- Precision@5, nDCG@5, MRR, Success@3
- 적합 결과 없음 정책

## 안전성

- 운영 DB는 identity 확인 후 read-only transaction의 SELECT만 사용
- Prisma schema와 migration 변경 없음
- 기존 Document, Chunk, DB embedding 변경 없음
- 공개 산출물에 원문·Attachment 본문·연락처를 포함하지 않음
- 30건 벡터 파일은 `.local` 아래에 두고 Git에서 제외

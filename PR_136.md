# feat(frontend): MOIRA Studio LLM 기획서 초안 생성

## 관련 이슈

- Closes #136

## 작업 개요

MOIRA Studio의 기존 입력 화면에서 기획 메모를 받아 Gemini API로 프로그램 기획서 초안을 생성하고, 수정 화면에 바로 반영하는 1차 기능을 구현했습니다.

## 작업 내용

- 기존 스튜디오 입력 UI는 유지하고 생성 흐름만 연결
- Gemini API 호출용 서버 라우트 추가
- 기획서 고정 출력 포맷 JSON 파싱 및 본문 변환 로직 추가
- 초안 결과를 편집 화면에 전달하는 sessionStorage 연결 추가
- Gemini 모델을 무료 등급 우선 순서로 재시도하도록 구성

## 구현 범위

### 포함

- `/studio` 입력 화면 기반 LLM 초안 생성
- Gemini 응답 파싱 및 편집 화면 반영
- 무료 모델 우선 시도 및 실패 시 fallback 처리

### 제외

- 초안 저장/버전 관리 DB 연동
- 히스토리 영구 저장
- UI 레이아웃 전면 개편

## 테스트 방법

1. `apps/frontend/.env`에 `GEMINI_API_KEY` 설정
2. `apps/frontend`에서 `npm run dev` 실행
3. `/studio`에서 기획 메모 입력 후 `기획안 만들기` 클릭
4. 생성된 초안이 `/studio/document/demo-document-1` 편집 화면에 반영되는지 확인

## 스크린샷

- 별도 문서에 추가 예정: [PR_136_screenshots.md](PR_136_screenshots.md)

## 참고사항

- 기존 UI를 그대로 사용하고, 생성 동작만 연결했습니다.
- 무료 등급 모델이 안 맞는 경우를 대비해 모델 우선순위 fallback을 넣었습니다.

## 관련이슈

- #136
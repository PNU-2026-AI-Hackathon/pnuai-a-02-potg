# feat(be): 계정·프로필 조회·수정 API 구현

## 관련 이슈

- Closes #108

## 작업 개요

기존 Prisma `User` 모델을 활용해 로그인 사용자가 자신의 계정·프로필 정보를 조회하고 수정할 수 있는 Express API를 구현했습니다.

현재 `User`, `Interest`, `UserInterest` 모델만으로 요구사항을 충족할 수 있어 Prisma schema와 migration은 변경하지 않았습니다. 기존 마이페이지 UI와 레이아웃도 그대로 유지합니다.

## 작업 내용

### 데이터 계약

- 사용자·관심 분야 모델과 canonical migration 정합성 검토
- 프로필 조회·수정 필드 확정
- 필드별 validation, null 처리, 개인정보·캐시 정책 정의
- 별도 `UserProfile` 모델을 추가하지 않는 근거 문서화
- 기존 관심 분야 API 재사용 방향 문서화

### Backend

- Express `/api/me` 라우터 등록
- `GET /api/me/profile` 구현
- `PATCH /api/me/profile` 구현
- `authenticateJwt` 적용
- JWT `sub`로 조회된 `req.user.id`만 사용자 식별에 사용
- 명시적 Prisma `select`로 응답 필드 제한
- 허용된 수정 필드만 별도 Prisma `data` 객체로 구성
- password 미조회·미반환
- 프로필 응답에 `Cache-Control: no-store` 적용
- Prisma 오류와 개인정보를 응답 및 로그에 노출하지 않음

## API

### GET `/api/me/profile`

인증된 사용자의 상세 프로필을 조회합니다.

응답 필드:

```text
id
userId
name
email
accountType
gender
birthDate
region
phone
createdAt
updatedAt
```

- `birthDate`: UTC 기준 `YYYY-MM-DD`
- `createdAt`, `updatedAt`: ISO 8601
- password 및 관심 분야 relation 미포함

### PATCH `/api/me/profile`

수정 가능 필드:

```text
name
gender
birthDate
region
phone
```

읽기 전용 필드와 알 수 없는 필드는 `400 INVALID_PROFILE_FIELD`로 거부합니다.

주요 validation:

- `name`: trim 후 1~50자
- `gender`: `FEMALE`, `MALE`, `OTHER`, `null`
- `birthDate`: 실제 존재하며 미래가 아닌 `YYYY-MM-DD` 또는 `null`
- `region`: trim, 최대 100자, 제어 문자 금지, 빈 값은 `null`
- `phone`: 하이픈·공백 제거 후 숫자 8~15자리, 빈 값은 `null`
- 빈 수정 요청: `400 EMPTY_PROFILE_UPDATE`

## 오류 응답

```json
{
  "code": "ERROR_CODE",
  "error": "Safe error message"
}
```

주요 오류 코드:

```text
INVALID_BODY
INVALID_PROFILE_FIELD
EMPTY_PROFILE_UPDATE
INVALID_NAME
INVALID_GENDER
INVALID_BIRTH_DATE
INVALID_REGION
INVALID_PHONE
AUTHENTICATION_REQUIRED
USER_NOT_FOUND
PROFILE_LOOKUP_FAILED
PROFILE_UPDATE_FAILED
```

## 구현 범위

### 포함

- 프로필 데이터 계약 문서
- 본인 프로필 조회·부분 수정 API
- 입력 validation 및 정규화
- 개인정보 보호 및 캐시 방지

### 제외

- 기존 마이페이지 UI 및 레이아웃 변경
- Next.js 프로필 프록시
- 관심 분야 UI 변경
- 비밀번호·이메일·사용자 ID 변경
- 회원 탈퇴
- 프로필 이미지와 공개 프로필
- 게시글·댓글·좋아요·북마크
- 활동 통계 및 행사 추천
- 관리자 사용자 관리

## 관심 분야

관심 분야는 이번 PR에서 변경하지 않습니다. 후속 연동 시 기존 API를 재사용합니다.

```text
GET /api/interests
GET /api/interests/me
PUT /api/interests/me
```

중복되는 `/api/me/interests` endpoint는 추가하지 않았습니다.

## 검증 결과

통과:

- Backend TypeScript build
- 프로필 validation 및 정규화 검사
- 생년월일 UTC 직렬화 검사
- password 미포함 검사
- 읽기 전용·알 수 없는 필드 차단 검사
- `git diff --check`
- Prisma schema·migration 무변경 확인
- 기존 마이페이지 및 `next-env.d.ts` 무변경 확인

실제 DB 통합 테스트는 안전한 로컬 또는 disposable DB 연결 정보가 확인되지 않아 실행하지 않았습니다. 운영 DB에는 접속하거나 데이터를 변경하지 않았습니다.

## 수동 테스트 방법

1. 안전한 테스트 DB에 테스트 사용자를 준비합니다.
2. Backend에 테스트 DB의 `DATABASE_URL`과 `JWT_SECRET`을 설정합니다.
3. 로그인 API로 JWT를 발급받습니다.
4. JWT로 `GET /api/me/profile`을 호출해 본인 프로필과 직렬화 형식을 확인합니다.
5. `PATCH /api/me/profile`로 허용 필드를 수정합니다.
6. 다시 조회해 변경값이 유지되는지 확인합니다.
7. 읽기 전용 필드와 잘못된 입력이 `400`인지 확인합니다.
8. 인증 없이 호출했을 때 `401`인지 확인합니다.

## Prisma 및 DB 변경

```text
UserProfile 모델 추가: 없음
schema.prisma 변경: 없음
migration 생성·수정: 없음
DB 직접 변경: 없음
```

기존 `User` 모델에 필요한 필드가 모두 존재하고 PK 조회에 필요한 index도 이미 있으므로 추가 DB 구조 변경이 필요하지 않습니다.

## 참고 사항

- 기존 마이페이지 UI와 메인페이지 UI는 변경하지 않았습니다.
- password는 Prisma 조회 및 API 응답에 포함되지 않습니다.
- 실제 DB 통합 테스트는 안전한 테스트 환경에서 추가 확인이 필요합니다.
- Next.js 프록시와 마이페이지 실데이터 연동은 기존 UI 보존 방침에 맞춰 별도 후속 작업으로 진행해야 합니다.

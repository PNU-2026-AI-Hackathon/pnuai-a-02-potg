# 프로그램 게시판 의미 검색 파일럿 결과

## 대상과 방법

- 대상: 정제된 텍스트 프로그램 17건
- 모델: `nlpai-lab/KURE-v1` (기존 로컬 캐시, 1024차원)
- 검색 단위: 프로그램 1건당 벡터 1개
- 유사도: 정규화 벡터의 코사인 유사도
- 평가 질의: 사람이 기대 프로그램을 지정한 자연어 질의 7개

회차별 활동, 신청기간, 접수 상태, 전화번호, 상세 주소는 임베딩하지 않았다. 검색된 프로그램의 상세 자료는 이후 `sourceId`로 조회하는 구조다.

## 필드 조합 비교

| 검색 프로필 | Hit@1 | Hit@3 | Hit@5 | MRR |
| --- | ---: | ---: | ---: | ---: |
| 제목 | 1.000 | 1.000 | 1.000 | 1.000 |
| 제목 + 소개·목표 | 0.857 | 1.000 | 1.000 | 0.905 |
| 제목 + 소개·목표 + 대상 | 0.857 | 1.000 | 1.000 | 0.929 |

## 해석

7개 질의에서는 세 조합 모두 기대 프로그램을 Top 3 안에 찾았다. 따라서 자연어 질의에서 관련 프로그램 후보를 수집하는 기본 파이프라인은 동작한다.

다만 제목 기준선이 가장 높다는 결과를 곧바로 “제목만 사용해야 한다”로 해석하면 안 된다. 현재 평가 질의와 프로그램 제목의 어휘가 가깝고, 17건 중 독서회 계열이 다수인 작은 편향 표본이기 때문이다. 소개·목표·대상을 추가하면 검색 문서가 길어져 특정 짧은 질의에서는 의미가 희석될 수도 있다.

대표 실패 사례는 `크리스마스 장식을 직접 만드는 체험`이다.

- 제목: `크리스마스 리스 만들기` 1위
- 제목 + 소개·목표: `크리스마스 리스 만들기` 3위
- 제목 + 소개·목표 + 대상: `크리스마스 리스 만들기` 2위

샌드아트 체험 프로그램도 `크리스마스`, `체험` 표현을 강하게 공유해 상위에 왔다. 그러나 기대 프로그램은 모든 조합에서 Top 3 안에 포함되었다.

또한 `성인이 한 권의 책을 읽고 이야기를 나누는 독서 모임`에서는 제목+소개+대상 조합의 2~3위에 아동 그림책 프로그램이 포함됐다. 대상 한 줄이 항상 검색 순위를 충분히 통제하지는 않는다는 뜻이다. 전체 데이터 확장 시에는 대상·연령대를 임베딩에만 의존하지 않고 구조화 필터 또는 후순위 재정렬 조건으로 사용하는 방안을 검토해야 한다.

## 현재 판단

- Top N 후보 수집 용도로 의미 검색을 계속 검증할 가치는 있다.
- 현재 17건에서는 Top 3 회수율이 100%였지만 표본이 작아 최종 성능 근거로는 부족하다.
- UI 기본 프로필은 기획 의도와 정보량을 고려해 `제목 + 소개·목표 + 대상`으로 유지한다.
- 351건 확대 전에 제목과 겹치지 않는 표현, 복합 조건, 모호한 요청을 포함한 평가 질의를 추가해야 한다.
- 이후에는 의미 검색 Top N을 얻은 뒤 프로그램별 강의계획서·회차 정보를 조회하여 LLM 참고 자료로 제공한다. 회차별 내용 자체를 별도 임베딩할 필요는 현재 확인되지 않았다.

## 대상 호환성 재정렬

초기 검색에서 `성인 독서 모임` 질의의 상위 결과에 유아 프로그램이 포함되는 문제가 확인되어 `audience-compatibility-v1` 재정렬을 추가했다.

- 질의에서 성인, 유아, 어린이, 초등 저학년, 초등학생, 청소년, 누구나·지역주민을 구분한다.
- 프로그램의 정제된 대상 필드를 동일한 분류로 해석한다.
- 코사인 유사도 원점수는 변경하지 않고 대상 일치·포함·충돌에 따른 별도 조정값으로 순위만 재정렬한다.
- UI에는 의미 유사도와 대상 호환성 근거를 각각 표시한다.

`성인이 한 권의 책을 읽고 이야기를 나누는 독서 모임` 재검증 결과 Top 5가 모두 성인 또는 지역주민 독서회로 구성됐으며, 유아 전용 프로그램은 대상 불일치 감점으로 제외됐다.

## 주제·활동 재정렬과 최소 기준

대상만 일치하는 영어 수업이 환경·만들기 질의의 2위로 노출되는 사례를 반영해 `topic-activity-compatibility-v1`을 추가했다.

- 주제: 환경·기후, 영어, 독서, 그림책, 인문학, 크리스마스
- 활동: 만들기·공예, 토론, 공연, 언어 학습, 강연
- 질의와 프로그램에서 동일한 사전으로 개념을 판정한다.
- 코사인 유사도와 별도로 개념 일치·누락 조정값을 계산한다.
- 명시된 개념이 1~2개이면 모두 충족해야 최소 기준을 통과한다. 세 개 이상이면 67% 이상을 요구한다.
- 최종 재정렬 점수 0.45 미만은 제외한다.
- 따라서 적합한 결과가 부족하면 요청한 Top N보다 적게 반환한다.

대표 검증 결과는 다음과 같다.

| 질의 | 최소 기준 통과 | 첫 결과 | 비고 |
| --- | ---: | --- | --- |
| 초등 저학년 환경·기후 만들기 | 1건 | 4354 똑똑한 지구인 프로젝트 | 영어 수업 제외 |
| 아이와 그림책·클레이 활동 | 3건 | 4351 클레이로 만나는 그림책 이야기 | 그림책과 만들기를 모두 포함한 자료만 통과 |
| 초등 저학년 영어 수업 | 1건 | 4353 Cool Summer! Cool English! | 영어·언어 학습 모두 일치 |

검색 결과에는 `detailed`, `partial`, `basic` 상세도와 감지된 회차 수도 함께 반환한다. 위 세 질의에서 4351·4353·4354는 목표와 4회차 교육 내용이 있는 `detailed`로 판정됐다.

세 질의의 LLM 전달용 Markdown은 `apps/backend/.local/program-board-search/contexts/`에 생성한다. 상세 원문이 없는 프로그램은 소개까지만 포함하며 회차 정보를 임의로 보충하지 말라는 지침을 함께 전달한다.

## 첨부 정제 표본 20건 확장

텍스트 정제 17건과 첨부 정제 20건의 `sourceId` 중복이 없음을 확인하고 검색 말뭉치를 총 37건으로 확장했다.

- 텍스트 프로그램: 17건
- 첨부 정제 프로그램: 20건
- 통합 검색 문서·벡터: 37개
- 첨부 20건은 모두 목표 또는 소개와 회차별 강의계획서를 가지고 있어 `detailed`로 판정
- 첨부 프로그램의 Markdown에는 정제된 회차, 활동, 교수방법, 준비물을 최대 8,000자 안에서 포함
- 검색 결과 링크는 텍스트 프로그램 상세와 첨부 검수 상세 화면으로 구분

기존 7개 질의에 첨부 프로그램용 질의 5개를 추가해 총 12개를 평가했다.

| 기본 프로필 | Hit@1 | Hit@3 | Hit@5 | MRR |
| --- | ---: | ---: | ---: | ---: |
| 제목 + 소개·목표 + 대상 | 91.7% | 100% | 100% | 0.958 |

기대 결과가 1위가 아니었던 항목은 `크리스마스 장식을 직접 만드는 체험` 한 건이며, 기대한 리스 만들기는 2위였다. 나머지 기존·첨부 질의 11개는 기대 프로그램이 1위였다.

첨부 표본 대표 검증:

- 초등 저학년 과학 실험 만들기 → 2488 `나는야 꼬마 과학자`
- 성인 생활 영어 → 2697 `매체를 통해 배우는 생활 영어`
- 초등 고학년 보드게임 → 2701 `보드게임이랑 놀자`
- 성인 그림책 감정 테라피 → 2484 `마음과 만나는 그림책 테라피`
- 초등 저학년 파닉스 영어 읽기 → 2703 `Reading with phonics`

초등 저학년 과학 질의에 초등 4~6학년 자료가 의미적으로 유사해 함께 노출되는 문제를 발견해, 명시적인 저학년·고학년 충돌은 점수 감점이 아니라 최소 기준 탈락으로 처리했다.

## 재현 명령

```bash
cd apps/backend
npm run program-board-search:profiles
node scripts/run-program-board-semantic-search.js build --profile title
node scripts/run-program-board-semantic-search.js build --profile title+intro
node scripts/run-program-board-semantic-search.js build --profile title+intro+target
npm run program-board-search:evaluate
npm run program-board-search -- --query "초등학생 환경 만들기 수업" --limit 5 --profile title+intro+target
```

모델 실행은 기존 `.model-cache`를 오프라인으로 사용한다. 임베딩과 평가 JSON은 `apps/backend/.local/program-board-search/`에 생성되며 Git에 포함하지 않는다.

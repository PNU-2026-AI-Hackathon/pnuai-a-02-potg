# 프로그램 첨부 4단계 병합·중복 제거 결과

## 수행 내용

3단계 대표 HWP/PDF 20건의 선택 구간을 기존 본문 정제 결과와 병합했다.

- 기존 라벨 사전과 프로그램 기본정보 재사용
- 첨부의 회차를 `session`, `date`, `activity`, `materialsOrNotes` 공통 구조로 변환
- 프로그램 소개와 운영 정보 구획에 첨부 신규 정보 추가
- 기본정보·본문과 동일한 첨부 항목 제거
- PDF/HWP 병합 셀에서 생긴 표 머리글 잡음 제거
- 첨부 전체 추출문은 최종 게시 데이터에서 제외하고 별도 검수 파일에만 보존
- 값이 다른 경우 덮어쓰지 않고 검수 경고로 남기는 구조 마련

## 실행 방법

```powershell
cd apps/backend
npm run program-attachment-merge:samples
$env:PROGRAM_BOARD_CRAWL='.local/geumjeong-small-library-crawl/<crawl-file>.json'
$env:PROGRAM_ATTACHMENT_ENRICHMENT='.local/program-attachment-enrichment/samples.json'
npm run test:program-attachment-merge
```

결과는 `.local/program-attachment-enrichment/merged-samples.json`에 생성된다.

## 대표 20건 결과

| 항목 | 결과 |
| --- | ---: |
| 병합 프로그램 | 20건 |
| 자동 검토 후보 | 17건 |
| 수동 검토 필요 | 3건 |
| 회차가 구조화된 프로그램 | 20건 |
| 구조화된 전체 회차 | 160개 |
| 첨부에서 새로 추가된 항목 | 65개 |
| 기본정보·본문과 중복되어 제거된 항목 | 67개 |
| 표 머리글 잡음으로 폐기된 항목 | 16개 |
| 해결되지 않은 값 충돌 | 4개 |

`자동 검토 후보`는 자동 게시 승인이 아니다. 병합 규칙이 명확하게 적용됐으며 다음 검수 UI에서 사람이 확인할 수 있는 상태라는 의미다.

## 병합 예시

### 알콩달콩 책놀이 세상

- 기본정보와 같은 프로그램명·대상·장소·재료비 등은 반복하지 않음
- 첨부에만 있는 상세 교육기간, 학습자 준비물, 강의실 준비를 운영 정보에 추가
- 4회차 활동을 공통 회차 구조로 추가

### 일본어 초급반

- 8회차 날짜와 활동을 공통 구조로 변환
- 상세 교육일시, 운영횟수, 교재, 교육내용을 추가
- 강사와 대상 등 기본정보 중복은 제거

### 자녀 독서지도·생각 쑥쑥 그림책

- 공유 PDF의 2페이지와 3페이지를 각각 선택
- 8회차씩 구조화
- 게시글과 첨부 제목이 다른 경우 `첨부 표기명`으로 보존

### 통합 PDF의 비용 정보

- `교재비 5,000원`, `교재비 10,000원`처럼 숫자가 명확한 값은 운영 정보에 추가
- `차시·세부 교육내용·교수방법`처럼 PDF 표 머리글이 준비물로 잘못 읽힌 값은 폐기하고 감사 기록에 남김

## 중복 제거 원칙

1. 제목·대상·강사·모집인원 등 기본정보와 동등한 첨부 값은 표시하지 않는다.
2. 교육기간이나 장소에 더 상세한 시간·휴강일·대면 전환 장소가 있으면 추가한다.
3. 첨부에서만 확인되는 교재·준비물·비용·프로그램 설명은 해당 구획에 추가한다.
4. 첨부 제목이 다르지만 프로그램 구간 매칭이 명확하면 `첨부 표기명`으로 보존한다.
5. 값이 충돌하면 자동으로 덮어쓰지 않고 `MANUAL_REVIEW_REQUIRED`로 보낸다.
6. 첨부 전체 원문은 게시판 데이터에 복사하지 않는다.

## 검수 피드백 반영 규칙

1. 비대면·대면 전환·코로나 상황·교재 및 재료 배부 안내는 프로그램 소개가 아니라 `이용 안내 > 운영 안내`로 이동한다.
2. 대상은 근거가 있는 경우 `초등학생`, `중학생`, `고등학생`과 학년을 함께 표시한다.
3. 재료비·교재비가 `없음`, `0원`, `ㅡ`로 명시되면 기본정보에서 칸을 생략하지 않고 `없음`으로 표시한다.
4. HWP의 `온라인 가능 여부` 체크박스는 `온라인 진행 가능` 기본정보로 보존한다. 이는 `온라인 접수 여부`와 다른 항목이다.
5. 회차 일자가 없을 때 교육 시작일~종료일의 7일 간격 날짜 수와 회차 수가 정확히 같을 때만 날짜를 자동 보완한다. 휴강 등으로 일치하지 않으면 추정하지 않고 UI에서 일자 열을 숨긴다.
6. `ㆍ`, `·`, `․`로 이어진 활동은 줄바꿈 글머리표로 표시하고, 안전하게 확정할 수 있는 붙임말만 사전 기반으로 띄어쓰기를 보정한다.
7. `(참고)` 같은 표 꼬리말은 게시 데이터에서 제거한다.
8. 참고도서·회차 제목·도서 표지처럼 HWP 도형이나 이미지 안에 있어 텍스트 추출기가 읽지 못한 값은 생성하지 않는다. 검수 UI에 원본 대조 경고를 표시하고 후속 HWP 임베디드 이미지 추출/OCR 대상으로 남긴다.

## HWP 임베디드 이미지 실험

- `사고력 쑥쑥 독서 논술(2483)` HWP에서는 임베디드 이미지가 발견되지 않았다.
- 실제 이미지 포함 표본은 `마음과 만나는 그림책 테라피(2484)`였으며, 참고도서 표지 8장을 추출했다.
- HWP 표 구조에서 각 표지가 1~8회차의 `비고` 셀에 하나씩 연결된 사실을 확인했다.
- 같은 행의 `참고 도서` 셀에서 도서명도 함께 복원하여 `referenceBooks`, `referenceImages`로 병합한다.
- 검수 UI는 `교수방법`, `준비물`, `비고`를 분리하고 값이 존재하는 열만 표시한다. 참고도서와 표지 이미지는 `비고` 열에서 명확히 구분한다.
- 이미지와 회차의 표 셀 연결이 확인되지 않는 문서는 자동 배치하지 않고 원본 대조 대상으로 남긴다.

## 표 열 의미 보존 규칙

- 표의 마지막 열을 일괄적으로 준비물로 취급하지 않는다. 원문 머리글이 `교육내용`이면 활동 내용에 합치고, `준비물` 또는 `비고`일 때만 우측 칸에 보존한다.
- PDF는 평탄화된 텍스트만으로 열 경계를 추측하지 않고 원본 표 셀 구조를 우선 사용한다.
- `생각 쑥쑥 그림책(2699)`의 `종합장과 필기도구` 같은 준비물은 원문 `준비물` 열에서 복원한다.
- `책과 나를 연결하는 하브루타(2700)`와 `보드게임이랑 놀자(2701)`는 공유 PDF의 4·5페이지 표 셀을 읽어 각각 8회차로 복원한다.
- 수업 변경 가능 문구와 장애인 전화접수 문구는 프로그램 소개가 아닌 `이용 안내 > 운영 안내`로 분류한다.
- PDF 표의 `교수방법`, `준비물`, `비고`는 하나의 칸으로 합치지 않고 독립 필드로 보존하며, 값이 있는 열만 UI에 표시한다.
- 병합 셀은 행 범위를 따라 값을 상속한다. 예를 들어 `Society`가 1~2회차에 병합되어 있으면 두 회차의 분류로 보존하고, `Princess Hyacinth`와 `King Bidgood in bathtub`는 각각의 활동으로 분리한다.
- PDF 표 안의 이미지 수와 회차 수가 일치하고 페이지 순서가 확인되면 각 회차의 `referenceImages`로 연결한다. 불일치하면 자동 배치하지 않는다.
- `프로그램명`과 같은 행 또는 인접 셀에 있는 `목표`, `프로그램 소개`, `강의목표`는 중복 제거 전에 셀 경계로 분리한다.

## 알려진 한계

- `어린이 과학탐구교실(2702)`은 병합된 `차시(날짜)` 머리글을 해석해 8회차를 복원했다.
- `I Love story(2704)`는 같은 이름의 정상 첨부 주소를 대체 근거로 사용해 공유 PDF 8페이지의 8회차를 복원했다.
- `생각톡톡! 미술아 놀자(2706)`는 첨부 표기명 `생각 톡톡! 창의력up! 미술아 놀자!`의 핵심 제목 단어를 비교해 10~11페이지의 8회차를 복원했다.
- 현재 결과는 대표 20건에 한정하며 전체 HWP/PDF 대상에 적용하지 않았다.
- 자유문의 문장 단위 소개·이용 안내 분류는 첨부에서 명확한 라벨이 있는 정보가 우선이다.
- 실제 UI에서의 가독성과 분류 위치는 다음 검수 단계에서 사람이 확인해야 한다.

## 다음 작업

대표 20건 검수 UI에서 다음 내용을 나란히 보여준다.

1. 원사이트 본문과 링크
2. 첨부파일 및 선택된 페이지
3. 첨부에서 선택한 프로그램 구간
4. 중복 제거·추가·폐기·충돌 감사 내역
5. 회차별 활동 공통 구조
6. 최종 프로그램 게시판 미리보기

# feat: 정제 프로그램 의미 검색 및 AI 기획서 생성 파일럿

## 작업 배경

정제된 프로그램 정보를 대상으로 사용자의 자연어 요구와 유사한 프로그램을 찾고, 검색된 프로그램의 상세 정보를 근거로 신규 프로그램 기획서 초안을 생성할 수 있는지 검증합니다.

이번 PR은 전체 351건 적용 전 파일럿입니다. 텍스트 정제 17건과 첨부 정제 표본 20건을 합친 총 37건을 검색 대상으로 사용합니다.

## 주요 변경 사항

### 1. 프로그램 검색용 데이터 통합

- 텍스트 정제 프로그램 17건과 첨부 정제 프로그램 20건을 하나의 검색 코퍼스로 통합했습니다.
- 프로그램마다 제목, 소개·목표, 대상 정보를 조합한 검색 프로필을 생성합니다.
- 첨부 정제 프로그램은 회차별 활동과 준비물·비고 등 상세 정보도 검색 결과 후속 컨텍스트에 포함할 수 있습니다.

### 2. 의미 검색 및 호환성 재정렬

- `nlpai-lab/KURE-v1` 임베딩을 로컬에서 생성하고 프로그램 단위 벡터로 검색합니다.
- 검색 프로필을 `title`, `title+intro`, `title+intro+target` 세 종류로 비교할 수 있습니다.
- 의미 유사도와 별개로 대상, 주제, 활동 호환성을 판정하여 결과를 재정렬합니다.
- 대상 학년이 명시적으로 충돌하는 결과는 제외합니다.
- 최소 점수와 요청 개념 충족 기준을 적용하여 부적절한 결과로 Top N을 억지로 채우지 않습니다.

### 3. 상세도 평가와 Markdown 컨텍스트 생성

- 검색된 프로그램을 `detailed`, `partial`, `basic` 상세도로 분류합니다.
- 상위 결과의 기본 정보, 소개·목표, 회차별 내용을 LLM 전달용 Markdown으로 압축합니다.
- 원문에 없는 회차나 운영 조건을 임의로 보완하지 않도록 출처와 누락 상태를 명시합니다.

### 4. Gemini 기획서 생성 실험

- 기존 Gemini Studio 생성 API가 검색 결과 Markdown을 참고자료로 받을 수 있도록 확장했습니다.
- Gemini 3.6 Flash를 기본값으로 사용하며 3.5 Flash Lite, 3.1 Flash Lite도 선택할 수 있습니다.
- 참고 프로그램을 그대로 복제하지 않고 새로운 회차를 구성하도록 프롬프트를 보강했습니다.
- 날짜, 정원, 예산, 재료비처럼 근거가 없는 운영 값은 `미정(담당자 확정 필요)`으로 표시합니다.

### 5. 검수용 임시 UI

- `/programs/search-pilot`에서 자연어 검색, 검색 프로필과 Top N 선택, 결과별 유사도·재정렬 근거 확인이 가능합니다.
- 검색 결과로 생성한 Markdown 컨텍스트를 확인하고 Gemini 모델을 골라 기획서 초안을 생성할 수 있습니다.

## 검색 성능 검증

37건을 대상으로 고정 질의 12개를 평가했습니다.

| 검색 프로필 | Hit@1 | Hit@3 | Hit@5 | MRR |
| --- | ---: | ---: | ---: | ---: |
| title+intro+target | 91.7% | 100% | 100% | 0.958 |

- 12개 질의 모두 기대 프로그램이 Top 3 안에 포함됐습니다.
- 크리스마스 리스 질의 1건만 기대 결과가 2위였고, 나머지는 1위였습니다.
- 첨부 정제 검색 실험에서는 `초등 저학년이 직접 만들며 배우는 과학 실험 수업` 질의에 2488번 프로그램이 단독 적격 결과로 선택됐습니다.

## 생성 실험 결과

- 텍스트 정제 프로그램이 검색되는 세 가지 질의로 Markdown과 기획서 생성을 검증했습니다.
- 첨부 정제 프로그램 2488번의 8개 회차를 참고한 생성도 검증했습니다.
- 생성 결과는 출처의 주제와 구성 방식을 참고하되 회차를 그대로 복사하지 않았습니다.
- 근거가 없는 운영 정보는 확정값으로 생성하지 않고 담당자 확인 대상으로 남겼습니다.

상세 결과는 다음 문서를 참고합니다.

- `PROGRAM_BOARD_SEMANTIC_SEARCH_PILOT_RESULTS.md`
- `PROGRAM_BOARD_DRAFT_EXPERIMENT_RESULTS.md`

## 검증 방법

```bash
cd apps/backend
npm run test:program-board-semantic-search
npm run build

cd ../frontend
npx tsc --noEmit
npx eslint src/app/programs/search-pilot/ProgramBoardSearchClient.tsx src/app/programs/search-pilot/page.tsx src/app/api/studio/generate/route.ts src/lib/studio-draft.ts
```

검증 결과:

- 프로그램 의미 검색 테스트 6건 통과
- 백엔드 TypeScript 빌드 통과
- 프런트엔드 TypeScript 검사 통과
- 변경 파일 대상 ESLint 통과
- 검색 API에서 통합 후보 37건 확인
- 임시 UI HTTP 200 응답 확인

## 스크린샷

> 아래 영역에 GitHub PR 작성 시 스크린샷을 첨부해 주세요.

### 자연어 검색 결과와 상세도

<!-- 스크린샷 추가: 37건 검색 결과, 의미 유사도 및 재정렬 근거가 보이는 화면 -->

### LLM 전달용 Markdown 컨텍스트

<!-- 스크린샷 추가: Top N 프로그램 상세 정보가 압축된 Markdown 화면 -->

### Gemini 프로그램 기획서 생성 결과

<!-- 스크린샷 추가: 모델 선택과 생성된 기획서 초안이 보이는 화면 -->

## 제한 사항

- 현재 검색 대상은 전체 351건이 아니라 정제 품질을 확인한 37건입니다.
- 대상·주제·활동 호환성은 명시적인 분류 사전을 사용하므로 새로운 표현이 발견되면 규칙 보완이 필요합니다.
- 의미 유사도는 정답 확률이 아니며, 생성 결과도 사서의 최종 검수가 필요합니다.
- Gemini 생성은 API 키와 사용량 한도의 영향을 받습니다.
- 로컬 임베딩 및 검색 산출물은 재생성 가능한 파일이므로 커밋하지 않습니다.

## 후속 작업

- 파일럿 검색·생성 결과를 사용자 검수로 확정합니다.
- 전체 351건 정제가 완료되면 동일 파이프라인으로 검색 코퍼스를 확장합니다.
- 전체 데이터에서 검색 평가 질의를 늘리고 오탐 유형별로 분류 사전을 보완합니다.
- 프로그램 게시판 정식 UI와 기획서 작성 흐름을 연결합니다.

## 체크리스트

- [x] 텍스트 정제 17건 검색 적용
- [x] 첨부 정제 20건 검색 적용
- [x] 대상·주제·활동 호환성 재정렬
- [x] 최소 결과 기준 적용
- [x] 상세도 평가 및 Markdown 컨텍스트 생성
- [x] Gemini 기획서 생성 실험
- [x] 임시 검수 UI 구현
- [x] 테스트·빌드·정적 검사 통과
- [ ] PR 본문에 검수 화면 스크린샷 첨부
- [ ] 전체 351건 확장 여부 결정

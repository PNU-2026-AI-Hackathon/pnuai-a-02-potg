import { NextRequest,NextResponse } from 'next/server';import { getBackendUrl } from '@/lib/backend-url';export const dynamic='force-dynamic';
async function proxy(request:NextRequest,context:{params:Promise<{path:string[]}>}){const {path}=await context.params;if(!path?.length||path.some(x=>x.includes('/')||x.includes('\\')||x==='..'))return NextResponse.json({error:'Invalid path'},{status:400});const target=new URL(getBackendUrl(`/api/internal/program-case-search/${path.map(encodeURIComponent).join('/')}`));request.nextUrl.searchParams.forEach((v,k)=>target.searchParams.append(k,v));try{const init:RequestInit={method:request.method,cache:'no-store',headers:{'Content-Type':'application/json'}};if(request.method==='POST')init.body=await request.text();const response=await fetch(target,init);return new NextResponse(await response.arrayBuffer(),{status:response.status,headers:{'Content-Type':response.headers.get('content-type')??'application/json','Cache-Control':'no-store'}})}catch{return NextResponse.json({error:'Search backend is unavailable'},{status:503})}}
export const GET=proxy;export const POST=proxy;

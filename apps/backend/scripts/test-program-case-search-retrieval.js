const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const {tokenize}=require('../dist/services/programCaseSearchRetrieval/tokenizer');const {buildBm25,searchBm25}=require('../dist/services/programCaseSearchRetrieval/bm25');const {metrics}=require('../dist/services/programCaseSearchEvaluation/evaluation');
assert.deepEqual(tokenize('  초등학생, AI-교실!  '),['초등학생','ai','교실']);const records=[{corpusId:'a',groupId:'g1',lexicalText:'환경 체험 환경'},{corpusId:'b',groupId:'g2',lexicalText:'독서 토론'}];const index=buildBm25(records,'fixture');assert.equal(searchBm25(index,'환경')[0].corpusId,'a');assert.equal(new Set(index.documents.map(x=>x.groupId)).size,2);const m=metrics();assert.ok(['AWAITING_HUMAN_LABELS','PROVISIONAL'].includes(m.status));console.log(JSON.stringify({passed:true,tokenizer:index.version,documents:index.documentCount,metricsStatus:m.status}));

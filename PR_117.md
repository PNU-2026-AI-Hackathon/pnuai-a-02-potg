# feat(ai-search): Attachment 구조 보존 Representation 및 ProgramCase 구간 후보 기반 구축

## 관련 이슈

- Closes #117
- Parent: #113
- Depends on: #114

## 작업 개요

Issue #114에서 생성한 검증된 content-addressed binary snapshot을 입력으로 PDF, Image OCR, HWP의 parser-native 구조를 보존하고, 이를 기반으로 Attachment section 후보와 설명 가능한 ProgramCase 후보를 생성하는 Representation Layer를 구축했습니다.

```text
Verified Binary Snapshot
→ Parser-native Structural Representation
→ Derived Visual Block
→ Derived Program Section Candidate
→ Explainable ProgramCase Candidate
```

Search Corpus, Search Document, embedding, BM25, Dense/Hybrid 검색과 최종 ProgramCase-section 연결은 생성하지 않습니다.

## 입력 데이터

| 유형 | 고유 binary | Attachment reference |
|---|---:|---:|
| PDF | 12 | 55 |
| JPEG | 83 | 125 |
| PNG | 19 | 31 |
| HWP | 22 | 26 |
| 합계 | 136 | 237 |

Source dataset hash:

```text
16c7135e1620dd07c9be3b57bcbb60865a34dec2ef19c55438f839f0e73a2e9c
```

## 주요 변경

### 공통 Representation 계약

- `program-case-attachment-representation/v1` 계약 추가
- `PARSER_NATIVE`와 `DERIVED` origin 분리
- source SHA-256, binary snapshot ref, parser/version, provenance 유지
- stable JSON과 content hash 기반 결정적 record ID
- parser/representation version과 dangling reference validation
- `.local/program-case-search-v2/representation` artifact store 추가

### PDF Representation

- PDF.js page와 text item 구조 보존
- page text와 문자 통계
- `TEXT`, `LOW_DENSITY`, `OCR_CANDIDATE` 판정
- text item 순서, transform, width/height, font 정보 보존
- page hash와 OCR 후보 page 기록

실제 snapshot 결과:

```text
PDF: 12/12 성공
page: 63
text item: 7,109
OCR candidate page: 1
```

### HWP Representation

- kordoc 4.2.7 Markdown block order 보존
- paragraph와 table 분리
- table row/cell 및 rowspan/colspan 보존
- 원래 순서를 재구성할 수 있는 `structuralOrder`
- 원본 heading style을 확정하지 않는 derived heading candidate

실제 snapshot 결과:

```text
HWP: 22/22 성공
paragraph: 62
table: 29
row: 443
cell: 1,483
heading candidate: 53
```

### 구조화 Image OCR

- CLOVA OCR field order, text, confidence, polygon, lineBreak 보존
- 인증정보와 endpoint를 제외한 `ocr-response.safe.json` 생성
- field → line → visual block 결정적 파생
- 동일 source/provider/parser/representation artifact 재사용
- 외부 실행에 명시적 flag, source hash와 호출 상한 요구
- retry 0 실행 지원

승인된 고유 image snapshot 9건 표본 결과:

```text
실제 CLOVA 호출: 9
성공/실패: 9/0
retry: 0
field: 2,599
line: 925
visual block: 56
polygon 누락: 0
lineBreak 보유: 2,599/2,599
```

전체 image 102건은 실행하지 않았습니다. 저장된 9개 safe response 재실행에서는 artifact 9/9를 재사용했고 추가 API 호출은 0이었습니다.

### Visual Block 역할과 Reading Order

Visual block과 program section을 분리했습니다.

Block 역할 후보:

```text
PROGRAM_CONTENT
TITLE_CANDIDATE
PROGRAM_METADATA
TABLE_OR_GRID
HEADER_OR_BRANDING
CONTACT_OR_FOOTER
ADMINISTRATIVE_NOTICE
UNKNOWN
```

Reading order 후보:

```text
COLUMN_MAJOR
ROW_MAJOR
HYBRID_LAYOUT
UNRESOLVED
```

역할과 reading order는 deterministic rule, confidence, evidence, classifier version과 함께 기록합니다.

### Section Candidate

- visual block을 그대로 program section으로 승격하지 않음
- 단일 linked ProgramCase image는 강한 복수 프로그램 근거가 없으면 whole-document section 하나 유지
- 표 행, cell gap, vertical gap만으로 분할하지 않음
- header/footer/contact는 보존하되 독립 program section에서 제외
- 공유 image는 반복되는 제목형 line과 근접 metadata가 있을 때만 분할
- `includedBlockRefs`, `excludedPeripheralBlockRefs`, boundary evidence 기록

9개 표본 재검증에서 단일 linked ProgramCase 5개는 모두 section 1개로 안정화됐습니다. 최대 공유 포스터는 예상 약 6개 중 4개 section을 탐지해 추가 rule 개선이 필요합니다.

### ProgramCase Candidate

- 후보군을 snapshot manifest의 `linkedProgramCaseIds`로 제한
- title/target/date/location/keyword evidence 분리
- `CANDIDATE`, `AMBIGUOUS`, `NO_RELIABLE_MATCH`만 생성
- 최종 연결 판정은 생성하지 않음
- title token을 복사하던 keyword evidence 이중 점수 제거
- threshold는 변경하지 않음

## CLI

```powershell
npm.cmd run program-case-attachment-representation -- --dry-run
npm.cmd run program-case-attachment-representation -- --build-pdf
npm.cmd run program-case-attachment-representation -- --build-hwp
npm.cmd run program-case-attachment-representation -- --plan-ocr
npm.cmd run program-case-attachment-representation -- --build-ocr
npm.cmd run program-case-attachment-representation -- --build-sections
npm.cmd run program-case-attachment-representation -- --build-candidates
npm.cmd run program-case-attachment-representation -- --validate
```

외부 OCR 호출에는 다음이 모두 필요합니다.

```text
--build-ocr
--allow-external-api
--source-hash <sha256>
--max-calls <1..10>
```

## 검증

통과:

- Backend TypeScript build
- Representation fixture/회귀 테스트
- Source snapshot 계약·build·CLI 회귀 테스트
- PDF page 순서/hash/type/text item 검증
- OCR field order/polygon/confidence 보존
- lineBreak 및 coordinate fallback line 생성
- visual block role/reading order 검증
- HWP paragraph/table/row/cell/병합 구조 검증
- 단일 program + 여러 visual block → section 1개
- footer/header → 독립 section 미생성
- 반복 title + metadata → 복수 section
- vertical gap only → section 미분할
- linked ProgramCase 범위 외 candidate 미생성
- 동일 safe response 재실행 artifact hash 동일
- dangling reference 0
- parser/provenance 누락 0
- `git diff --check`

현재 local representation dataset hash:

```text
d16a025967991b46176d00397f6e0b81fc48a231d4464b913104873f0824be3b
```

## 안전성

- 외부 URL 재다운로드 없음
- #114 local snapshot만 사용
- snapshot binary 수정 없음
- 운영 DB write 없음
- Prisma schema/migration 변경 없음
- 기존 OCR 결과 변경 없음
- 기존 Document/Chunk/Embedding 변경 없음
- Search Corpus 및 검색 기능 생성 없음
- OCR secret, 인증 header와 endpoint artifact 저장 없음
- OCR 원문·연락처·강사명 등은 `.local`에만 저장
- 공개 문서에는 집계와 안전한 품질 판정만 기록

## 현재 한계와 후속 작업

표본 검증의 최종 권고는 다음과 같습니다.

```text
C. 공유 포스터 section 전략을 한 번 더 수정한 뒤 같은 safe artifact로 재검증
```

단일 프로그램 image의 과분할은 해소됐지만 다음은 후속 보완이 필요합니다.

- 공유 포스터의 column-local title-to-body 연결
- 큰 visual block 내부 title anchor recall
- 공유 일정 section과 ProgramCase title의 설명 가능한 연결
- 최대 공유 포스터의 의미 구간 4개 → 약 6개 복원

전체 고유 image 102건 실행은 별도 승인 전까지 수행하지 않습니다. 저장된 9개 safe artifact만으로 section 규칙을 반복 검증할 수 있습니다.

## 문서

- `docs/specs/PROGRAM_CASE_ATTACHMENT_REPRESENTATION.md`
- `docs/analysis/PROGRAM_CASE_ATTACHMENT_REPRESENTATION.md`

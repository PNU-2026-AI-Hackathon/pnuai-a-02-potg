# 기획서 틀을 정하고 항목 단위 수정을 붙인다

## 배경

#170에서 사서가 조건을 고르면 유사 사례를 찾아 Markdown으로 만들고, 그것을 참고 자료로
LLM이 기획안 초안을 생성하는 데까지 이어졌다.

지금 초안은 항목이 일곱 개뿐이고 회차가 문자열 목록이다. 이 상태로는 두 가지를 못 한다.

- 개발계획서가 정한 **항목 단위 수정**. 「1주 차 활동 내용만 바꿔줘」를 하려면 회차가
  각각 독립된 값이어야 하는데 지금은 문자열 배열이라 몇 번째가 몇 회차인지 알 수 없다.
- **HWP·PDF 내보내기**. 사서가 결재 문서로 쓰려면 실제 강의계획서처럼 표가 있어야 한다.

수정 단위를 정하는 일이 곧 기획서 항목을 정하는 일이라 두 가지를 함께 다룬다.

## 지금 상태

### 생성되는 초안

```ts
StudioDraft {
  title, summary, target, duration,
  details: string[],       // 회차별 활동이 문자열 목록으로 들어온다
  expectedEffects,
  notes: string[],
  content: string          // 위 필드를 렌더링한 전체 텍스트
}
```

`buildStudioPrompt`가 이 일곱 필드를 JSON으로 요구하고, `parseStudioDraft`가 검증한다.

### 이미 있는 수정 API

`/api/studio/revise`가 있지만 **선택한 텍스트를 고치는 방식**이다.

```ts
StudioReviseRequest { documentId, selectedText, instruction, context: { title, before, after } }
```

개발계획서가 말한 항목 단위 수정과 설계가 다르다.

### 정제한 351건이 실제로 갖고 있는 항목

기획서 틀은 지어내지 않고 실제 강의계획서에서 관찰한 것을 쓴다.

| 항목 | 351건 중 등장 |
| --- | ---: |
| 대상 · 강사 · 모집인원 · 교육기간 · 신청기간 | 351 |
| 교육시간 | 350 |
| 운영 도서관 | 323 |
| 목표 | 108 |
| 학습자 준비물 | 96 |
| 강의실 준비 | 79 |
| 재료비 | 72 |
| 장소 | 69 |

회차는 244개 프로그램에 1,759개가 있고, 그중 일자가 붙은 것이 977개, 비고가 있는 것이
245개, 준비물이 따로 적힌 것이 34개다.

## 이번에 풀 문제

### 1. 진실을 어디에 둘지 정해야 한다

`StudioDraft`에 구조화된 필드와 `content: string`이 함께 있다. 지금은 둘이 따로 놀아서,
텍스트를 고치면 필드와 어긋나고 필드를 고치면 텍스트를 다시 만들어야 한다.

### 2. 회차가 문자열 목록이라 한 회차만 고칠 수 없다

```
지금:  details: ["1회차: 독서의 즐거움 발견 …", "2회차: 문장 수집과 필사 …"]
필요:  sessions: [{ session: 1, activity: "…", materials: "…", notes: "…" }, …]
```

정제 351건이 쓰는 회차 구조와 같은 모양이라 새로 설계할 필요는 없다.

### 3. 수정할 때 무엇을 다시 보낼지 정해야 한다

개발계획서가 이 기능을 둔 목적이 **API 사용량 절감**이다. 고칠 때마다 참고 사례
Markdown을 통째로 다시 보내면 취지가 없어진다.

## 작업 범위

### 포함

- [ ] 기획서 항목 정의 (351건에서 관찰한 구조 + 개발계획서가 정한 항목)
- [ ] `StudioDraft`를 그 구조로 바꾸고 회차를 객체 배열로
- [ ] 생성 프롬프트와 파서를 새 구조에 맞춤
- [ ] 항목 단위 수정 API (기존 `revise`를 확장할지 새로 둘지 결정)
- [ ] 스튜디오 화면에서 항목을 골라 수정 요청
- [ ] 회차를 표로 보여주기
- [ ] PDF 내보내기

### 제외

- 351건 DB 적재 (다음 이슈)
- 포스터 생성
- 검색·생성 로직 변경 (#170에서 확정, 그대로 쓴다)

## 판단이 필요한 것

### (1) `content: string`을 어떻게 할까

| 안 | 내용 | 장점 | 단점 |
| --- | --- | --- | --- |
| A | 구조화 필드를 진실로 삼고 `content`는 매번 만들어 씀 | 항목 수정과 내보내기가 자연스럽다 | 기존 저장분을 옮겨야 한다 |
| B | `content`를 진실로 삼고 필드는 화면 표시용 | 지금 저장분을 그대로 쓴다 | 항목 단위 수정이 사실상 불가능 |
| C | 둘 다 유지하고 동기화 | 당장 안 깨진다 | 어긋나는 순간을 못 막는다 |

**A를 제안한다.** 이 이슈의 목적이 항목 단위 수정과 서식 있는 내보내기인데 둘 다
구조가 진실이어야 가능하다.

### (2) 기존 `revise` API를 어떻게 할까

| 안 | 내용 |
| --- | --- |
| A | 항목 단위로 새로 만들고 선택 텍스트 방식은 지운다 |
| B | 둘 다 둔다. 항목 수정과 문장 다듬기는 쓰임이 다르다 |
| C | 항목 단위로 바꾸고 선택 텍스트는 그 항목 안에서만 |

**B를 제안한다.** 「홍보문만 친근하게」와 「이 문장만 쉽게」는 다른 요청이고, 선택 텍스트
방식은 이미 동작한다. 다만 선택 텍스트 수정도 **어느 항목 안인지**는 알아야 구조가 깨지지 않는다.

### (3) 어떤 항목을 넣을까

개발계획서가 정한 것과 351건에서 관찰한 것을 합쳐야 한다. 아래는 초안이며 확정이 필요하다.

```
프로그램명 · 기획 의도 · 대상 · 운영 기간 · 운영 횟수 · 모집 인원 · 장소
목표
회차별 활동   (회차 | 일자 | 활동 내용 | 준비물 | 비고)
준비물 · 재료비 · 강의실 준비
기대 효과
홍보문
운영 시 유의점
```

351건에는 없지만 개발계획서에 있는 것이 **기획 의도 · 홍보문 · 기대 효과**다.
기존 프로그램은 이미 만들어진 것이라 그런 항목을 남기지 않는데, 새로 기획할 때는 필요하다.

### (4) 내보내기를 무엇으로 할까 — **PDF로 정함**

먼저 PDF만 만든다. 만들기 쉽고 어디서나 열린다. HWP는 사서가 실제로 그 형식으로
결재를 올린다는 것이 확인되면 그때 붙인다.

## 검증 방법

```bash
npm run dev --prefix apps/backend
npm run dev --prefix apps/frontend
```

`/studio` 에서

1. 조건을 고르고 기획안을 만든다
2. 회차가 표로 보이는지 확인한다
3. 한 항목만 골라 수정 요청을 넣고, 나머지 항목이 그대로인지 확인한다
4. 내보내기를 눌러 파일을 연다

성공 기준

- 회차를 하나만 고쳐도 다른 회차와 항목이 그대로다
- 수정 요청에 참고 사례 Markdown을 다시 보내지 않는다
- 내보낸 파일에 회차가 표로 들어간다

## 참고

- 생성 프롬프트와 파서: `apps/frontend/src/lib/studio-draft.ts`
- 기존 수정 API: `apps/frontend/src/app/api/studio/revise/route.ts`
- 351건이 실제로 갖는 항목: `PROGRAM_FULL_NORMALIZATION_PARTIAL_RESULTS.md`

## 관련 이슈

- 선행: #146 (전체 정제), #169 (351건 검색 확장)
- 후속: 351건 DB 적재, 이용자용 게시판 UI

Closes #171

# 프로그램 검색 기반 LLM 기획서 생성 실험 결과

## 실험 조건

- 생성 모델: `gemini-3.6-flash`
- 검색 대상: 정제 텍스트 프로그램 17건
- 검색 방식: KURE-v1 의미 검색 + 대상·주제·활동 호환성 재정렬
- 최소 기준: 재정렬 점수 0.45 이상, 명시된 주제·활동 개념 충족
- 참고 입력: 검색 결과의 기본 정보·소개·목표·회차 원문을 압축한 Markdown
- 생성 형식: 제목, 개요, 대상, 기간, 4회차 세부 내용, 기대 효과, 참고 사항

초기 생성에서는 참고 회차를 거의 그대로 복제하거나 근거 없이 예산 지원을 확정하는 문제가 발견됐다. 프롬프트에 원문 활동 복제 금지와 미제공 운영값의 `미정(담당자 확정 필요)` 표기 규칙을 추가한 뒤 세 질의를 다시 생성했다.

## 최종 세 질의 결과

### 1. 초등 저학년 환경·기후 만들기

- 검색 참고: 4354 `똑똑한 지구인 프로젝트`
- 생성 제목: `생각이 싹트는 초록 지구 탐험대`
- 결과: 4회차 신규 환경 만들기 활동 생성
- 운영값: 모집 인원·기간·예산을 미정으로 표시
- 판단: 파이프라인 검증에 사용 가능
- 검수 메모: 일부 재료 표현과 문장 중복(`입체 입체`)은 사람의 최종 교정이 필요함

### 2. 아이와 그림책·클레이 활동

- 검색 참고: 4351 `클레이로 만나는 그림책 이야기` 등 적합 자료 3건
- 생성 제목: `조물조물 그림책 세상: 아이와 함께 만드는 클레이 이야기`
- 결과: 식습관·자연·이웃·감정 주제의 4회차 신규 구성
- 운영값: 모집 인원·일시·재료비를 미정으로 표시
- 판단: 사용자 요청의 `아이와 함께`를 보호자 동반으로 해석했으므로 실제 의도 확인 UI가 후속으로 필요함

### 3. 초등 저학년 영어 수업

- 검색 참고: 4353 `Cool Summer! Cool English!`
- 생성 제목: `영어 동화와 함께하는 언어 놀이: Story & Play English`
- 결과: 동물·음식·감정·도전 주제의 4회차 신규 구성
- 운영값: 모집 인원·시간·비용을 미정으로 표시
- 판단: 참고 구조를 활용하면서 새 활동을 제안해 실험 목적에 부합함

## 종합 판단

검색 결과의 상세 원문을 Markdown으로 압축해 LLM에 전달하고 정형 기획서를 생성하는 전체 흐름은 동작한다. 4351·4353·4354처럼 목표와 회차 원문이 있는 자료에서는 4회차 기획안 생성에 충분한 참고 근거가 있었다.

다만 자동 생성물을 바로 확정 문서로 사용하면 안 된다.

- 재료·활동의 현실성 및 안전성은 사서 검토가 필요하다.
- 문장 중복이나 어색한 재료 조합이 발생할 수 있다.
- `아이와 함께`처럼 대상 관계가 모호한 요청은 생성 전에 사용자 확인이 필요하다.
- 기존 회차를 과도하게 복제하지 않는지 원문 대비 검수가 필요하다.
- 날짜·인원·예산처럼 근거 없는 운영값은 미정 상태로 유지해야 한다.

따라서 현재 단계의 적절한 제품 역할은 `완성 기획서 자동 작성`이 아니라 `근거가 표시된 편집 가능한 기획서 초안 생성`이다.

## 임시 UI

`/programs/search-pilot`에서 다음 순서로 직접 실험할 수 있다.

1. 자연어 질의 입력
2. 최소 기준을 통과한 검색 결과와 상세도 확인
3. `Top 결과로 Markdown 만들기` 선택
4. 생성 모델 선택
5. `LLM 기획서 생성 실험` 선택
6. Markdown과 생성 기획서를 같은 화면에서 비교

선택 가능한 모델은 `Gemini 3.6 Flash`, `Gemini 3.5 Flash Lite`, `Gemini 3.1 Flash Lite`이며 기본값은 `Gemini 3.6 Flash`다.

## 생성 산출물

- Markdown: `apps/backend/.local/program-board-search/contexts/`
- JSON 기획서: `apps/backend/.local/program-board-search/drafts/`

로컬 생성 산출물은 API 응답과 원문을 포함하므로 Git에 커밋하지 않는다.

## 첨부 정제 프로그램 생성 검증

37건 확장 후 `초등 저학년이 직접 만들며 배우는 과학 실험 수업`으로 전체 흐름을 추가 검증했다.

- 최소 기준 통과: 2488 `나는야 꼬마 과학자` 1건
- 참고 자료: 목표와 정제된 8회차 과학 실험·만들기 내용
- 생성 모델: `gemini-3.6-flash`
- 생성 제목: `손끝으로 만나는 창의 과학: 만들며 배우는 어린이 과학 교실`
- 생성 결과: 우주·풍력·자석·착시·진동·밀도·회로·균형을 다루는 신규 8회차
- 모집 인원·일정·비용·장소: `미정(담당자 확정 필요)`로 유지

첨부에서 정제한 회차 정보가 Markdown과 LLM 입력에 정상적으로 포함됐으며, 기존 활동을 그대로 복사하지 않고 새로운 과학 활동으로 재구성됐다. 이로써 첨부 정제 데이터도 검색과 기획서 생성의 참고자료로 사용할 수 있음을 확인했다.

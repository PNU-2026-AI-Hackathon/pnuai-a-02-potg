# feat: 텍스트+첨부 프로그램 198건의 첨부 보강 판정 파이프라인 구축

## 작업 배경

#140에서 본문 텍스트 또는 HTML 표만으로 구성된 프로그램 17건의 규칙 기반 정제와 게시판 UI 적용을 완료했다.

다음 대상은 본문 텍스트와 이미지·HWP·PDF 등의 보조 자료가 함께 존재하는 약 198건이다. 이 유형은 본문만으로 기본적인 게시판 화면을 만들 수 있더라도, 회차별 활동·세부 커리큘럼·선정도서·준비물처럼 이용자에게 중요한 정보가 첨부 자료에만 들어 있을 수 있다.

따라서 본문 글자 수나 라벨 개수만으로 첨부를 생략해서는 안 된다. 본문만으로 게시 가능한지와 첨부 자료까지 확인하여 정제가 완료됐는지를 별도로 판정해야 한다.

## 목표

텍스트와 첨부가 함께 있는 프로그램을 대상으로 다음 과정을 자동화한다.

1. 본문을 기존 `program-board/v1` 정제 파이프라인으로 처리한다.
2. 첨부파일 유형과 처리 가능 여부를 판별한다.
3. HWP·HWPX·PDF·이미지에서 텍스트와 표 정보를 추출한다.
4. 본문과 첨부 자료를 비교하여 신규 정보만 게시판 데이터에 병합한다.
5. 자동 처리가 불확실한 항목만 사람 검수 대상으로 분리한다.

이 작업에서 말하는 `본문만으로 게시 가능`은 첨부를 확인하지 않아도 된다는 뜻이 아니다. 기본 화면을 임시로 구성할 수 있다는 의미이며, 첨부가 있는 게시글은 첨부 확인 전까지 최종 정제 완료 상태가 될 수 없다.

## 대상 범위

- 전체 크롤링 데이터 351건 중 `text_with_supplement`로 분류된 약 198건
- 본문과 함께 존재하는 다음 보조 자료
  - 본문 내부 이미지 및 포스터
  - JPG·PNG 등 이미지 첨부
  - HWP·HWPX 첨부
  - 텍스트 PDF
  - 스캔 또는 이미지 기반 PDF
- 기존 `src/services/attachment/` 추출 모듈의 재검증 및 금정구 프로그램 정제 파이프라인 연결

## 제외 범위

- 이미지와 첨부만 존재하고 본문 텍스트가 없는 레코드의 전체 처리
- OCR 외부 API의 운영 과금 정책 확정
- 운영 DB 반영 및 배포
- 사서용 프로그램 등록 화면 구현
- 사람이 직접 모든 레코드를 처음부터 재작성하는 방식

## 판정 모델

### 1. 게시 가능 상태

본문만으로 현재 게시판의 기본 화면을 구성할 수 있는지 판정한다.

- `TEXT_READY`: 기본 정보와 표시 가능한 프로그램 소개가 있어 임시 게시 가능
- `TEXT_PARTIAL`: 기본 정보는 있으나 프로그램 내용이 부족함
- `TEXT_INSUFFICIENT`: 첨부를 읽지 않으면 프로그램 내용을 구성하기 어려움

이 상태는 첨부 확인 완료 여부와 무관하다.

### 2. 첨부 확인 상태

- `ATTACHMENT_UNCHECKED`: 첨부파일을 아직 분석하지 않음
- `ATTACHMENT_ENRICHED`: 첨부에서 새로운 프로그램 정보를 추출하여 병합함
- `ATTACHMENT_NO_NEW_INFO`: 첨부를 확인했으나 본문과 같거나 게시판에 추가할 정보가 없음
- `ATTACHMENT_REQUIRED`: 실질적인 프로그램 내용이 첨부에 있어 추출 결과가 필요함
- `MANUAL_REVIEW_REQUIRED`: 추출 실패, 낮은 OCR 신뢰도, 복잡한 표 또는 정보 충돌로 사람 확인 필요

최종 정제 완료는 `ATTACHMENT_UNCHECKED`가 아닌 경우에만 가능하다.

## 게시판에 보강할 정보

첨부 자료에서 다음 항목을 우선 추출한다.

- 프로그램 목적 및 소개
- 회차별 일자와 활동 내용
- 세부 커리큘럼 및 수업 방법
- 선정도서 및 교재
- 재료비와 준비물
- 상세 운영 장소
- 신청 시간과 신청 방법
- 문의처
- 취소·대기·개인정보 등 이용 안내

대상·강사·교육기간·교육시간·신청기간·장소·비용·모집인원처럼 기본 정보와 동일한 값은 반복 표시하지 않는다. 첨부에 더 구체적인 시간, 주소 또는 비용 조건이 있으면 추가 정보는 보존한다.

## 병합 원칙

1. 공공예약 기본 정보는 기본 필드의 기준값으로 유지한다.
2. 본문과 첨부에서 동일한 내용이 반복되면 한 번만 표시한다.
3. 첨부에서만 확인되는 회차별 활동과 커리큘럼은 프로그램 내용에 추가한다.
4. 기본 정보와 첨부 값이 충돌하면 임의로 덮어쓰지 않고 근거와 함께 검수 대상으로 보낸다.
5. 모집인원처럼 기존에 합의한 명확한 우선순위 규칙이 있는 필드는 해당 규칙을 적용한다.
6. 원문, 추출문, 정제 결과의 출처를 추적할 수 있도록 근거 정보를 보존한다.

## 처리 순서

### 1단계: 198건 목록과 첨부 인벤토리 생성

- 레코드별 본문 길이와 구조화 라벨 수
- 첨부 개수, 파일명, 확장자, URL
- 본문 내부 이미지 유무
- 현재 게시 가능 상태
- 필요한 추출 방식

재현 명령:

```powershell
cd apps/backend
npm run program-attachment-inventory:build
$env:PROGRAM_BOARD_CRAWL='.local/geumjeong-small-library-crawl/<crawl-file>.json'
npm run test:program-attachment-inventory
```

산출물은 `.local/program-attachment-inventory/inventory.json`에 생성하며, 원본 파일을 다운로드하거나 읽지는 않는다. 따라서 이 단계의 `TEXT_READY`는 임시 게시 가능성만 뜻하고 모든 첨부 상태는 `ATTACHMENT_UNCHECKED`로 유지한다.

### 2단계: 기존 첨부 모듈 재검증

- 다운로드와 파일 형식 판별
- HWP·HWPX 텍스트 및 표 추출
- PDF 텍스트 레이어 추출
- 이미지 PDF 판별과 페이지 렌더링
- 이미지 OCR 처리
- 실패 원인과 재시도 가능 여부 기록

단위 테스트 통과만으로 완료 처리하지 않고, 금정구 실제 첨부 표본으로 결과를 확인한다.

실제 표본 검증 명령:

```powershell
cd apps/backend
npm run program-attachment-samples:validate -- --per-type 2
```

결과는 `.local/program-attachment-validation/report.json`에 저장한다. 검증 과정은 실제 금정구 첨부를 다운로드하지만 원본 파일은 임시 디렉터리에서 처리한 뒤 삭제한다. 외부 OCR API는 호출하지 않으며, 이미지에 대해서는 형식·크기 검증과 OCR 입력용 전처리까지만 수행한다.

### 3단계: 비용이 낮고 정확한 형식부터 연결

1. HWP·HWPX 및 텍스트 PDF
2. 본문 내부의 HTML 표와 텍스트
3. 포스터 이미지 및 이미지 첨부 OCR
4. 스캔 PDF OCR

### 4단계: 본문·첨부 병합 및 중복 제거

- 기존 라벨 사전을 재사용한다.
- 회차별 활동은 가능한 경우 동일한 공통 구조로 만든다.
- 추출된 자유문은 프로그램 소개나 이용 안내로 분류한다.
- 첨부 전체 원문을 그대로 반복 표시하지 않는다.

### 5단계: 검수 UI 제공

각 표본에서 다음 내용을 한 화면에서 비교할 수 있어야 한다.

- 공공예약 원문 링크
- 원본 본문
- 원본 이미지 또는 첨부파일 링크
- 첨부에서 추출한 텍스트와 표
- 최종 게시판 정제 결과
- 신규 정보, 중복 제거, 충돌 및 누락 표시
- 자동 판정 상태와 검수 필요 사유

## 대표 표본 검증

198건 전체 처리 전에 첨부 유형별 대표 표본을 선정한다.

- 본문이 충분하고 포스터가 보조 역할만 하는 유형
- 본문보다 이미지에 회차별 내용이 많은 유형
- HWP 강의계획서에 세부 커리큘럼이 있는 유형
- 텍스트 PDF에 표가 있는 유형
- 스캔 PDF 또는 포스터 OCR이 필요한 유형
- 본문과 첨부 정보가 충돌하는 유형
- 첨부를 확인해도 새로운 정보가 없는 유형

대표 표본의 원본과 정제 결과를 사람이 승인한 후 전체 198건에 적용한다.

## 완료 조건

- [ ] `text_with_supplement` 약 198건의 입력 목록이 재현 가능한 명령으로 생성된다.
- [ ] 모든 대상에 게시 가능 상태와 첨부 확인 상태가 각각 기록된다.
- [ ] 첨부가 있는 레코드는 확인하지 않은 채 최종 정제 완료로 분류되지 않는다.
- [ ] HWP·HWPX·PDF·이미지별 처리 경로와 실패 사유가 기록된다.
- [ ] 회차별 활동·커리큘럼·선정도서·준비물 등 첨부의 신규 정보가 게시판 구조로 병합된다.
- [ ] 기본 정보·본문·첨부 사이의 중복이 제거된다.
- [ ] 정보 충돌이나 낮은 추출 신뢰도는 자동으로 사람 검수 대상으로 분류된다.
- [ ] 대표 표본에서 원본·추출 결과·최종 UI를 비교할 수 있다.
- [ ] 기존 텍스트형 17건의 정제 결과에 회귀가 없다.
- [ ] 실제 금정구 첨부 표본을 포함한 자동 테스트와 검수 결과가 문서화된다.

## 후속 작업

이 이슈가 완료되면 다음 순서로 별도 작업을 진행한다.

1. 이미지 또는 첨부만 존재하는 레코드 처리
2. OCR 신뢰도와 비용 정책 확정
3. 사람 검수·수정·승인 도구 구현
4. 정제 데이터를 DB와 실제 프로그램 게시판에 연결
5. 사서용 프로그램 등록 필수 필드와 추가 내용 입력 구조 설계

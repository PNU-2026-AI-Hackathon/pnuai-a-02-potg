# 금정구 작은도서관 프로그램 크롤링·정제 규칙

## 1. 목적과 현재 범위

- 금정구 공공예약 서비스의 작은도서관 프로그램을 MOIRA 프로그램 게시판 형식으로 정제한다.
- 원문을 삭제하거나 의미를 추측하지 않고, 규칙으로 확실히 판별되는 내용만 구조화한다.
- 현재 UI 검증 범위는 대표 20건이다. 351건 전체 일괄 정제·DB 반영은 아직 하지 않았다.
- LLM API는 사용하지 않는다.
- 정제하지 못한 원문과 원본 첨부파일은 검증 근거로 보존한다.

## 2. 원사이트의 세 구간

1. 기본정보: 제목부터 온라인 접수 여부 및 첨부파일까지의 표
2. 프로그램 내용: 기본정보 다음부터 `<안내 사항>` 직전까지
3. 안내사항: `<안내 사항>`부터 수강신청자 목록 직전까지

수강신청자 목록은 개인정보이므로 수집·표시 대상에서 제외한다.

## 3. 기본정보 정제

필드:

- 운영 도서관
- 프로그램명
- 대상
- 강사
- 모집인원
- 교육기간
- 교육시간
- 신청기간
- 온라인 접수 여부
- 비용

규칙:

- 제목의 `[대상/도서관]` 태그를 분리하고 도서관 별칭을 표준 명칭으로 변환한다.
- 날짜 범위는 시작일과 종료일로 분리한다.
- `온라인접수여부`가 이미지로 표현된 경우 이미지 `alt` 또는 `title`을 읽는다.
- UI의 `온라인 접수 여부` 라벨은 줄바꿈하지 않는다.
- 온라인 접수 상태는 향후 신청기간과 현재 시각으로 계산하는 파생 필드로 전환하는 것이 권장된다.

### 모집인원 충돌

- 기본정보와 본문에서 모집인원이 다르고, 본문에서 실제 수업·추첨 인원이 한 가지로 명확하게 발견되면 본문 값을 우선한다.
- 예: 기본정보 200명, 본문 `10명이 수업 가능한 강좌`, `10명을 추첨` → 최종 10명.
- 본문에서 서로 다른 모집인원이 여러 개 발견되면 자동 결정하지 않고 검수 대상으로 분류한다.
- 본문 우선 적용 시 `CAPACITY_OVERRIDDEN_BY_DETAIL`, 모호할 때 `CAPACITY_DETAIL_AMBIGUOUS` 경고를 기록한다.

## 4. 프로그램 내용 유형

### HTML 교육계획표

- 행과 셀을 평문으로 합치지 않는다.
- `rowspan`, `colspan`, 헤더 여부와 줄바꿈을 보존해 다시 표로 표시한다.
- 제목만 들어 있는 1행 1셀 표는 교육계획표로 취급하지 않는다.

### 항목명-값 자유문

- `❏ 운영일시 : ...`, `○ 장소 : ...` 같은 형식을 인식한다.
- 띄어 쓴 `대 상`, `장 소`, `수 강 료`도 같은 항목으로 본다.
- 다음 줄이 괄호 또는 보충문으로 이어지면 앞 항목 값에 합친다.
- 인식 항목은 프로그램 소개, 운영 정보, 신청 안내, 문의로 분류한다.

### 일반 문장

- 의미를 임의로 추측하지 않고 녹색 `프로그램 소개` 카드에 원문을 보존한다.

### 이미지

- 원본 이미지를 녹색 `안내 이미지` 카드에 표시한다.
- 현재 이미지 OCR 결과를 프로그램 정제에 자동 반영하지 않는다.

### 첨부파일만 있는 경우

- 파일 링크를 보존하고 첨부파일 확인 안내를 표시한다.
- PDF/HWP 내용 자동 추출은 다음 단계 후보이다.

## 5. 중복 제거

기본 원칙은 `기본정보와 완전히 같은 정보는 제거하고, 추가 정보가 있으면 보존`이다.

항상 기본정보 중복으로 제거하는 항목:

- 대상·교육대상
- 강사·강사명
- 모집인원·정원
- 수강료·교육비·참가비·재료비의 동일 금액
- 온라인 접수 여부

조건부 처리:

- 운영·교육기간: 기본정보와 같은 날짜뿐이면 제거한다. 상세 시간이 추가되면 보존한다.
- 신청·접수기간: 기본정보와 같은 날짜뿐이면 제거한다. `오전 10시` 등 시간이 추가되면 보존한다.
- 장소: 기본정보의 도서관명과 완전히 같으면 제거한다. 주소·층·강의실 등 추가 정보가 있으면 보존한다.
- 비용: 기본정보와 같은 금액은 제거한다. 입금방법·계좌 추후 안내·환불정책 등은 보존한다.
- 제목: 제목과 동일한 프로그램 소개 문장은 제거한다.

중복 제거는 원문을 삭제하는 것이 아니다. 원문은 접힌 `공공예약 본문 원문 전체 보기`에 보존한다.

## 6. 안내사항 정제

안내사항은 다음 순서의 주제 카드로 분류한다.

1. 장소·교통 안내
2. 비용 안내
3. 신청 유의사항
4. 취소·운영 안내
5. 개인정보·촬영 안내
6. 문의
7. 기타 안내

키워드로 확실히 분류할 수 없는 문장은 삭제하지 않고 기타 안내에 둔다. 추가 운영 정보와 같은 문장은 안내사항에서 한 번 더 표시하지 않는다.

## 7. 상세 UI 순서

원사이트 작성 순서와 관계없이 아래 순서로 고정한다.

1. 프로그램 기본 정보
2. 프로그램 내용
3. 추가 운영 정보
4. 이용 안내
5. 첨부파일
6. 공공예약 본문 원문 전체 보기
7. 신청하기

`신청하기`는 MOIRA에서 직접 접수하지 않고 해당 공공예약 원사이트를 새 창으로 연다.

## 8. 첨부파일 정제 권장안

1. HTML 텍스트와 표를 먼저 정제한다.
2. 텍스트 PDF는 로컬 파서로 문단·표를 추출한다.
3. HWP/HWPX는 로컬 파서로 문단·표를 추출한다.
4. 이미지 PDF와 포스터는 원본을 먼저 표시한다.
5. OCR을 도입한다면 신뢰도가 높은 결과만 자동 반영하고 나머지는 사람 검수로 보낸다.
6. 추출 결과와 원본 파일을 항상 함께 보존한다.

권장 상태:

- `AUTO_NORMALIZED`: 규칙으로 충분히 정제됨
- `PARTIAL`: 일부 정제, 원본 또는 첨부 확인 가능
- `NEEDS_REVIEW`: 값 충돌 또는 저신뢰 추출
- `ATTACHMENT_ONLY`: 첨부파일 확인 필요
- `EMPTY`: 본문과 첨부가 없음

## 9. 향후 사서 입력 스키마

필수 입력:

- 운영 도서관
- 프로그램명
- 대상
- 강사
- 모집인원
- 교육기간
- 교육시간
- 신청기간
- 비용

선택 입력:

- 프로그램 한 줄 소개
- 목표
- 운영방법
- 상세 장소
- 준비물
- 교재
- 차시별 교육내용
- 비용 추가 안내
- 신청 유의사항
- 취소·환불·폐강 정책
- 개인정보·촬영 안내
- 문의
- 이미지와 첨부파일
- 기타 프로그램 추가 안내

작성 화면에서 기본정보와 같은 자유문을 다시 입력하면 중복 경고를 표시하는 것이 권장된다.

## 10. 사람 검수 원칙

- 사람이 351건을 처음부터 다시 작성하는 방식은 피한다.
- 자동 정제 결과를 먼저 만들고, 새로운 형식 또는 저신뢰 레코드만 사람이 승인·수정한다.
- 한 사례를 수정할 때는 해당 글만 고치지 않고 동일 유형 전체에 적용할 규칙으로 일반화한다.
- 원문에 없는 내용을 추측해 만들지 않는다.

## 11. 텍스트형 우선 처리 범위

이번 이슈의 우선 구현 범위는 다음 조건을 모두 만족하는 레코드이다.

- 본문 텍스트 또는 HTML 표가 존재한다.
- 인라인 이미지가 없다.
- HWP, PDF, 이미지 등 첨부파일이 없다.
- 테스트 레코드가 아니다.

2026-08-12 전체 재크롤 351건을 이 기준으로 분류한 결과 17건이 선택되었다. 이 배치는 `program-text-only-prototype/v1` 데이터로 생성하며, 이미지·HWP·PDF 정제는 다음 단계로 미룬다.

## 12. 현재 검증 상태와 알려진 한계

- 대표 20건은 원사이트와 20/20 일치 확인을 거쳤다.
- 대표 20건 UI에서 표·이미지·자유문·안내사항 유형을 검증 중이다.
- 351건 전체 정제 품질은 아직 확정되지 않았다.
- 이미지 안의 글자와 이미지 PDF는 OCR 없이는 구조화할 수 없다.
- HWP/PDF의 복잡한 표는 추출 후에도 사람 검수가 필요할 수 있다.
- 규칙 기반 중복 제거는 의미가 완전히 같은지 보수적으로 판단해야 한다.

# feat: 정제 프로그램 17건 의미 검색 성능 검증

## 작업 배경

프로그램 기획 지원 기능은 다음 흐름을 목표로 한다.

1. 사용자가 원하는 프로그램을 자연어로 설명한다.
2. 기존 프로그램 중 의미가 유사한 상위 N개를 찾는다.
3. 선택된 프로그램의 강의계획서·회차별 활동을 참고 자료로 수집한다.
4. 후속 단계에서 LLM이 참고 자료를 바탕으로 새 기획서를 작성한다.

이번 이슈에서는 정제가 완료된 텍스트 프로그램 17건만 이용해 1~2단계가 의도대로 동작하는지 먼저 검증한다. 검색이 유효한지 확인하기 전에는 LLM 기획서 생성이나 전체 351건 색인을 진행하지 않는다.

현재 저장소에는 `ProgramCase` 대상 KURE-v1 임베딩·검색 기능이 있으므로, 새로운 유료 API나 별도 모델을 도입하지 않고 기존 로컬 임베딩 기반 구조를 재사용한다. 단, 기존 `ProgramCase` 검색 말뭉치와 이번 프로그램 게시판 17건의 산출물·API는 분리한다.

> 주의: 17건 중 벙개 독서회 계열이 여러 건 포함되어 있어 표본이 편향되어 있다. 이번 결과는 전체 351건의 최종 검색 성능을 보장하는 평가가 아니라, 검색 단위·필드 구성·화면·평가 방법을 검증하는 파일럿이다.

## 목표

- 정제 프로그램 17건을 각각 하나의 검색 문서이자 하나의 벡터로 만든다.
- 사용자의 자연어 질의를 임베딩하고 코사인 유사도로 상위 N개 프로그램을 찾는다.
- 검색 결과의 순위, 프로그램 요약 정보, 유사도 점수를 간단한 UI로 확인한다.
- 고정 평가 질의를 이용해 검색 결과가 상식적으로 타당한지 정량·정성 평가한다.
- 어떤 기본 정보 조합이 검색에 가장 유용한지 비교해 이후 351건 확장 기준을 정한다.

## 검색 단위와 데이터 구성

### 검색 단위

`프로그램 1개 = 검색 문서 1개 = 임베딩 1개`로 구성한다.

회차별 활동, 강의계획서 표, 준비물, 유의사항은 이번 검색 임베딩에 넣지 않는다. 이 정보는 유사 프로그램이 선택된 뒤 `sourceId`로 조회하여 LLM에 제공할 상세 참고 자료로 사용한다.

### 검색용 필드

다음처럼 의미 검색에 도움이 되는 프로그램 수준 정보만 정규화하여 사용한다.

```text
[제목] 똑똑한 지구인 프로젝트(1.5℃를 지키는 아이들)
[소개·목표] 자연과 환경의 소중함을 배우고 만들기 활동을 하는 프로그램
[대상] 초등학생 1~3학년
[분야·형태] 환경, 독서, 만들기
```

우선순위 필드는 다음과 같다.

1. 제목
2. 프로그램 소개·목표·운영 내용
3. 대상 및 연령대
4. 원문에 명시된 프로그램 분야·수업 형태(있는 경우만)
5. 운영 도서관(사용자가 장소 조건을 검색할 가능성을 확인할 때만 선택적으로 포함)

교육기간, 신청기간, 접수 상태, 모집 인원, 전화번호, 상세 주소처럼 프로그램의 의미보다 시점·행정 상태에 가까운 값은 임베딩 문서에서 제외한다. 이 값들은 검색 결과 카드의 필터·표시 정보로만 사용한다.

### 비교할 검색 문서 조합

동일한 17건으로 최소 다음 세 조합을 비교한다.

| 조합 | 임베딩 대상 | 목적 |
| --- | --- | --- |
| A | 제목 | 최소 기준선 |
| B | 제목 + 소개·목표 | 프로그램 주제 검색 효과 확인 |
| C | 제목 + 소개·목표 + 대상 | 대상 조건이 포함된 자연어 질의 효과 확인 |

기본 후보는 C로 하되 평가 결과를 보고 확정한다. 단순히 텍스트를 많이 넣을수록 좋다고 가정하지 않는다.

## 구현 범위

### 1. 17건 검색 프로필 생성

- `apps/backend/.local/program-board/programs.json`의 정제 프로그램 17건을 입력으로 사용한다.
- `sourceId`, 제목, 소개·목표, 대상 등 검색에 사용할 필드를 명시적으로 매핑한다.
- 누락 필드와 최종 임베딩 문자열을 확인할 수 있는 진단 결과를 만든다.
- 기존 280건 `ProgramCase` 검색 산출물을 덮어쓰거나 섞지 않는다.

### 2. 임베딩 생성 및 검색 API

- 기존 KURE-v1 로컬 임베딩 실행 경로와 캐시를 재사용한다.
- 프로그램별 벡터와 모델명·차원·입력 체크섬 등 재현에 필요한 메타데이터를 저장한다.
- 질의 임베딩과 프로그램 임베딩의 코사인 유사도를 계산한다.
- 자연어 질의와 `limit`을 받아 상위 N개를 반환하는 별도 API를 만든다.
- 기본 `limit`은 5로 하며 허용 범위를 검증한다.
- 빈 질의, 잘못된 limit, 임베딩 산출물 누락을 명확한 오류로 처리한다.

응답에는 최소 다음 값이 포함되어야 한다.

```json
{
  "query": "초등학생이 환경을 배우며 만들기 하는 수업",
  "limit": 5,
  "model": "KURE-v1",
  "searchProfile": "title+intro+target",
  "results": [
    {
      "rank": 1,
      "sourceId": 4354,
      "title": "똑똑한 지구인 프로젝트(1.5℃를 지키는 아이들)",
      "target": "초등학생 1~3학년",
      "summary": "...",
      "similarity": 0.82
    }
  ]
}
```

### 3. 최소 검색 UI

디자인 완성보다 검색 결과 검수가 목적이다.

- 자연어 질의 입력창
- 검색 버튼
- Top N 선택(기본 5)
- 적용된 검색 프로필 표시
- 결과별 순위, 제목, 대상, 짧은 소개, 원문/프로그램 상세 링크
- 코사인 유사도 원점수 표시

유사도를 백분율로 함께 표시할 수는 있지만 반드시 `정확도`가 아니라 `유사도`라고 표기한다. 예: `유사도 0.823 (82.3%)`. 이 값은 정답 확률이 아니다.

## 검색 품질 검증

UI에서 결과가 나오는 것만으로는 완료로 보지 않는다. 17건의 실제 내용을 바탕으로 사람이 기대 결과를 지정한 자연어 질의 세트를 먼저 작성하고 결과 순위를 기록한다.

초기 질의 예시는 다음과 같다. 구현 단계에서 각 프로그램 내용을 확인한 뒤 기대 `sourceId`를 확정한다.

- `초등 저학년이 환경과 기후를 배우면서 만들기도 하는 수업`
- `아이와 함께 그림책을 읽고 클레이 활동을 하는 프로그램`
- `여름에 초등학생이 영어를 재미있게 배우는 강좌`
- `성인이 한 권의 책을 읽고 이야기를 나누는 독서 모임`
- `어린이가 관람할 수 있는 토끼 인형극`
- `크리스마스 장식을 직접 만드는 체험`
- `대학생 또는 성인을 위한 인문학 강연`

각 질의에 대해 다음을 기록한다.

| 질의 | 기대 프로그램 | 조합 A 순위 | 조합 B 순위 | 조합 C 순위 | Top 5 결과 | 판정·비고 |
| --- | --- | ---: | ---: | ---: | --- | --- |

평가 결과에는 최소 다음 지표를 포함한다.

- `Hit@1`, `Hit@3`, `Hit@5`: 기대 프로그램이 상위 K개 안에 포함되는 비율
- `MRR`: 첫 기대 프로그램 순위의 역수 평균
- 질의별 수동 판정: 상위 결과가 왜 적절하거나 부적절한지
- 독서회 연속 게시물이 상위 결과를 과도하게 점유하는지 여부

17건처럼 작은 데이터에서는 점수 하나보다 실패 사례를 분석하는 것이 중요하다. 제목의 날짜·회차 표현 때문에 동일 계열만 몰리는지, 대상 정보가 주제를 압도하는지, 소개가 비어 있는 레코드가 불리한지 함께 기록한다.

가능하면 기존 키워드/BM25 검색과 Dense 검색 결과도 같은 질의로 비교한다. 이는 필수 구현을 복잡하게 만들기 위한 것이 아니라, 17건 규모에서 임베딩이 단순 키워드 검색보다 실제로 이점이 있는지 확인하기 위한 기준선이다.

## 테스트

- 입력 17건이 누락·중복 없이 정확히 17개 검색 문서로 생성되는지 확인한다.
- 프로그램 하나에 벡터가 하나만 생성되는지 확인한다.
- 회차별 활동과 신청·연락 정보가 임베딩 문자열에 포함되지 않는지 확인한다.
- 동일 입력으로 다시 생성할 때 검색 문서와 체크섬이 동일한지 확인한다.
- 코사인 유사도 정렬과 `limit` 적용이 정확한지 단위 테스트한다.
- 빈 질의와 잘못된 요청 값의 API 동작을 테스트한다.
- UI에서 질의, Top N, 결과 순위 및 유사도를 확인할 수 있는지 검증한다.
- 고정 평가 질의 전체를 실행하고 비교 보고서를 생성한다.

## 단계와 사용자 확인 지점

1. **검색 프로필 생성**
   17건별 검색 필드와 최종 임베딩 문자열을 표로 출력하여 사용자에게 확인받는다.
2. **임베딩·검색 로직 구현**
   CLI 또는 테스트로 대표 질의 결과와 유사도를 먼저 공유한다.
3. **검색 API 구현**
   Top N 응답 구조와 오류 처리를 검증한다.
4. **최소 UI 구현**
   사용자가 직접 여러 자연어 질의를 입력해 결과를 검수한다.
5. **평가 보고서 작성**
   필드 조합 A/B/C의 Hit@K·MRR과 실패 사례를 정리하고 351건 확대 여부를 결정한다.

## 완료 조건

- [ ] 정제된 프로그램 17건이 각각 하나의 검색 문서와 벡터로 생성된다.
- [ ] 검색 문서에 제목·소개/목표·대상 등 승인된 프로그램 수준 정보만 들어간다.
- [ ] 회차별 활동은 임베딩하지 않고 `sourceId`로 후속 조회할 수 있다.
- [ ] 자연어 질의로 상위 N개 프로그램을 검색하는 API가 동작한다.
- [ ] 최소 UI에서 순위, 프로그램 요약, 유사도를 확인할 수 있다.
- [ ] 유사도는 정확도나 정답 확률로 오인되지 않게 표시된다.
- [ ] 제목/소개/대상 필드 조합별 고정 질의 평가 결과가 문서화된다.
- [ ] 실패 사례와 17건 표본 편향이 기록된다.
- [ ] 기존 `ProgramCase` 검색 데이터와 기능에 회귀가 없다.
- [ ] 자동 테스트와 실행·재생성 명령이 문서화된다.

## 이번 이슈에서 하지 않는 일

- 전체 351건 임베딩 및 일괄 검색 적용
- 회차별 활동을 별도 청크·벡터로 임베딩
- 검색된 강의계획서를 LLM에 전달하여 새 기획서를 생성하는 기능
- 최종 프로그램 게시판 UI 디자인 완성
- 접수 상태·날짜·도서관 등의 복합 필터 구현
- 유료 임베딩 API 도입

## 후속 작업

이번 파일럿에서 검색 품질과 검색 필드 구성이 승인되면 다음 순서로 확장한다.

1. 정제가 완료된 전체 프로그램으로 동일 검색 프로필을 생성한다.
2. 필요하면 의미 검색과 키워드 검색을 결합한 Hybrid 검색을 적용한다.
3. Top N의 `sourceId`로 강의계획서·회차별 활동·준비물 등 상세 정보를 조회한다.
4. 상세 정보를 Markdown 참고 자료로 구성한다.
5. 사용자 요청과 검색 근거를 함께 LLM에 제공하여 새 프로그램 기획서를 생성한다.

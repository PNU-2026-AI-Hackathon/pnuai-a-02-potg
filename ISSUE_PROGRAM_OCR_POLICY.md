# OCR 정책 확정

전체 351건 중 250건이 `OCR_REQUIRED` 상태로 대기 중이다. 이 문서는 OCR을 어떤 조건으로 실행할지 정하기 위한 것이다. 정책이 확정되기 전까지 OCR API는 호출하지 않는다.

## 1. 대상 규모

| 항목 | 수 |
| --- | ---: |
| OCR 대기 레코드 | 250건 |
| 고유 파일 (URL 기준) | **244개** |
| 파일 참조를 접어서 줄인 호출 | 50회 |

| 구분 | 건수 | 성격 |
| --- | ---: | --- |
| 본문 있음 + 이미지 첨부 | 126건 | 본문만으로 이미 게시 가능. OCR은 회차 등 상세 보강용 |
| 본문 없음 + 이미지뿐 | 124건 | OCR 없이는 게시할 내용이 사실상 없음 |

파일 종류는 `.jpg` 124개, `.png` 31개, 확장자 없는 본문 인라인 이미지 89개다. 표본을 확인한 결과 인라인 이미지도 대부분 `홍보문`·`안내문` 같은 실제 포스터이며, 금정구 서버 업로드 경로를 쓴다. 다만 외부 CDN(`blog.kakaocdn.net`) 이미지가 섞여 있어 장식용이 포함될 가능성이 있다.

**스캔 PDF는 0건이다.** 문서 경로 83건은 모두 텍스트 레이어가 있어 OCR 없이 처리됐다. 따라서 OCR 대상은 전부 이미지이고, CLOVA General 기준 **이미지 1장당 1회 호출로 약 244회**다.

## 2. 이미 구현되어 있는 것

이 저장소에는 OCR 파이프라인이 이미 상당 부분 들어와 있다. 새로 만들 것은 많지 않다.

| 모듈 | 역할 |
| --- | --- |
| `services/attachment/clovaOcrClient.ts` | CLOVA OCR 호출. 타임아웃·재시도·429 처리 |
| `services/attachment/clovaOcrResponseParser.ts` | 응답 파싱. `averageConfidence` 계산 |
| `services/attachment/imageOcrProcessor.ts` | 이미지 OCR 처리 |
| `services/attachment/imagePreprocessor.ts` | OCR 입력용 전처리 |
| `services/attachment/imageOcrDryRunService.ts` | **API 호출 없이 대상 점검** |
| `services/attachment/imageOcrReuse.ts` | 체크섬이 같은 파일의 결과 재사용 |
| `services/attachment/imageOcrFailureDiagnostic.ts` | 실패 원인 분류 |
| `config/clovaOcr.ts`, `config/attachmentOcr.ts` | 설정값 |

### 이미 걸려 있는 안전장치

- `CLOVA_OCR_ENABLED`는 **미설정 시 `false`**다. 실수로 과금될 위험이 없다.
- 요청당 타임아웃 30초, 응답 5MB, 재시도 최대 1회(상한 2회)
- 이미지 긴 변 4,000px로 축소 후 전송
- 체크섬이 같은 파일은 기존 결과를 재사용

## 3. 없는 것 — 이번에 정해야 할 것

| 항목 | 현재 상태 |
| --- | --- |
| **최소 신뢰도** | `averageConfidence`를 계산만 하고 **어디서도 거르지 않는다** |
| **호출 총량 상한** | 요청 단위 제한만 있고 "총 N회 넘으면 중단"이 없다 |
| **재사용의 배치 연결** | 재사용 로직이 DB(`ProgramCaseAttachment`) 기준이라 `.local` JSON 배치와 연결돼 있지 않다 |
| **미달 시 처리 규칙** | 신뢰도 낮은 결과를 버릴지 검수로 보낼지 정해진 바 없다 |

## 4. 결정 항목

### (1) 엔진

CLOVA OCR General이 이미 구현되어 있다. 다른 엔진으로 바꾸면 클라이언트·파서·전처리를 새로 만들어야 한다.

**권장: CLOVA 유지.** 한글 포스터 정확도가 로컬 엔진(Tesseract 등)보다 뚜렷이 낫고, 이미 검증된 코드가 있다.

단가는 네이버 클라우드 콘솔에서 확인이 필요하다. 이 문서는 호출 횟수(244회)만 확정한다.

### (2) 비용 한도

호출 총량 상한을 설정값으로 추가한다. 상한에 닿으면 남은 대상을 `OCR_BUDGET_EXCEEDED`로 남기고 중단한다.

**권장: 300회.** 실제 필요분 244회에 재시도·실패 여유를 더한 값이다.

### (3) 최소 신뢰도

CLOVA는 필드마다 `inferConfidence`를 준다. 평균 신뢰도로 자를지, 필드 단위로 자를지 정해야 한다.

**권장: 평균 0.8 미만은 사람 검수로.** 포스터는 디자인 글꼴·세로쓰기·배경 위 글자가 많아 부분 오인식이 흔하다. 낮은 값을 자동 반영하면 잘못된 회차 정보가 게시된다.

### (4) 미달 시 처리

**권장: 버리지 않고 `MANUAL_REVIEW_REQUIRED`로 보낸다.** 추출문은 검수 근거 파일에 남겨 사람이 원본과 대조할 수 있게 한다. 이 프로젝트의 기존 원칙(값이 불확실하면 자동 반영하지 않고 검수로 보낸다)과 같다.

### (5) OCR 결과의 자동 반영 범위

**권장: OCR 결과는 자동 검토 후보로도 올리지 않고 전부 사람 검수를 거친다.** HWP·PDF는 문서 구조가 근거였지만 OCR은 픽셀 추정이라 근거의 성격이 다르다. 최소한 첫 배치에서는 전량 검수하고, 정확도가 확인되면 그때 완화한다.

### (6) 대상 범위

본문이 없는 124건은 OCR이 유일한 수단이라 우선순위가 높다. 본문이 있는 126건은 이미 게시 가능하므로 보강 성격이다.

**권장: 124건 먼저, 결과 확인 후 126건.** 한 번에 244회를 쓰지 않고 절반으로 정확도를 먼저 확인한다.

## 5. 실행 전 점검

정책이 확정되면 실제 호출 전에 드라이런으로 대상을 점검한다. `imageOcrDryRunService`가 이미 있으므로 API 호출 없이 다음을 확인할 수 있다.

- 다운로드 가능 여부
- 형식 판별 결과 (이미지가 아닌 파일 걸러내기)
- 전처리 후 크기
- 체크섬 중복으로 줄어드는 호출 수

파일명이 같은데 URL이 다른 경우가 있다(`스포원 행사 안내문.jpg` 4건). 다운로드 후 체크섬으로 접으면 244회보다 더 줄어들 수 있다.

## 6. 확정된 결정

2026-08-15 확정.

| 항목 | 결정 |
| --- | --- |
| **엔진** | CLOVA OCR General 유지. 이미 구현된 클라이언트·파서·전처리를 그대로 쓴다 |
| **호출 상한** | **300회.** 필요분 244회에 재시도·실패 여유를 더한 값. 상한 도달 시 남은 대상은 `OCR_BUDGET_EXCEEDED`로 남기고 중단 |
| **최소 신뢰도** | 평균 `inferConfidence` **0.8** |
| **미달 시 처리** | 버리지 않고 `MANUAL_REVIEW_REQUIRED`로 보낸다. 추출문은 검수 근거 파일에 보존해 원본과 대조할 수 있게 한다 |
| **자동 반영 범위** | **전량 사람 검수.** 신뢰도 0.8 이상이라도 자동 검토 후보로 올리지 않는다. OCR은 문서 구조가 아니라 픽셀 추정이라 근거의 성격이 다르다. 첫 배치 정확도가 확인되면 그때 완화를 재검토한다 |
| **대상 순서** | 본문 없는 124건 먼저 실행 → 결과 확인 → 본문 있는 126건. 상한 300회가 둘 다 감당하므로 2단계 사이에 추가 승인은 필요 없다 |
| **자격증명** | 발급 완료. `CLOVA_OCR_INVOKE_URL`·`CLOVA_OCR_SECRET`을 실행 환경에 넣는다 |

### 실행 전제

`CLOVA_OCR_ENABLED`는 기본값이 `false`다. 실제 호출은 이 값을 `true`로 두고 실행할 때만 발생한다. 자격증명과 활성화 값은 사용자가 직접 넣는다.

```powershell
$env:CLOVA_OCR_ENABLED='true'
$env:CLOVA_OCR_INVOKE_URL='<발급받은 호출 URL>'
$env:CLOVA_OCR_SECRET='<발급받은 시크릿>'
```

## 7. 확정 후 구현할 것

| 순서 | 작업 | 비용 발생 |
| ---: | --- | --- |
| 1 | `config/attachmentOcr.ts`에 호출 상한(`ATTACHMENT_OCR_MAX_CALLS`, 기본 300)과 최소 신뢰도(`ATTACHMENT_OCR_MIN_CONFIDENCE`, 기본 0.8) 추가 | 없음 |
| 2 | 신뢰도 미달 판정과 `MANUAL_REVIEW_REQUIRED` 연결 | 없음 |
| 3 | `OCR_BUDGET_EXCEEDED` 상태 추가 및 상한 도달 시 중단 | 없음 |
| 4 | 배치에서 체크섬 기준 재사용 연결 (현재 DB 전용 로직을 `.local` 배치에서도 사용) | 없음 |
| 5 | **드라이런 실행** — 다운로드·형식 판별·중복 접기 결과 확인 | **없음** |
| 6 | 124건 OCR 실행 | **발생** |
| 7 | 결과 확인 후 126건 실행 | **발생** |
| 8 | OCR 결과를 기존 정제 파이프라인(구간 매칭 → 회차 추출 → 병합)에 연결 | 없음 |
| 9 | 검수 UI에 OCR 결과·신뢰도 표시 | 없음 |

1~5는 과금 없이 진행할 수 있다. **6부터는 실제 비용이 발생하므로 별도 확인을 받고 실행한다.**

## 8. 드라이런 결과 (2026-08-15, API 호출 0회)

```powershell
cd apps/backend
npm run program-ocr-batch:dry-run
```

| 항목 | 결과 |
| --- | ---: |
| 대상 파일 (URL 기준) | 244 |
| **체크섬 기준 고유 파일** | **159** |
| 내용이 같아 접힌 파일 | 56 |
| 이미지가 아닌 파일 | 0 |
| 다운로드 실패 | 29 |
| **예상 실제 호출** | **159회** |

체크섬으로 접으니 244회에서 **159회로 35% 줄었다.** `스포원 행사 안내문.jpg`처럼 URL이 다르고 내용이 같은 파일이 많다. 확정한 상한 300회에 충분히 들어간다.

이미지가 아닌 파일은 0건이다. 확장자 없는 본문 인라인 이미지도 모두 실제 이미지였다.

### 다운로드 실패 29건 — 해결 완료

> 아래는 1차 드라이런 기록이다. 두 원인 모두 처리해 **2차 드라이런에서 실패 0건**이 됐다.
> 최종 수치는 이 절 끝의 `2차 결과`를 참고한다.

| 사유 | 파일 | 영향 레코드 | 성격 |
| --- | ---: | ---: | --- |
| `HOST_NOT_ALLOWED` | 21 | 17건 | 본문 이미지가 `blog.kakaocdn.net`에 있다. 다운로더 허용 호스트 목록에 없어 차단됐다 |
| `INVALID_URL` | 8 | 5건 | 본문에 `data:image/png;base64,…`로 박혀 있다. 내려받을 URL이 아니라 이미 본문에 들어 있는 데이터다 |

**두 경우 모두 해당 레코드의 유일한 이미지다.** 다른 정상 이미지를 함께 가진 레코드는 0건이라, 이 22건은 지금 상태로는 OCR을 해도 내용을 전혀 못 얻는다. 그중 10건은 본문조차 없다.

- `HOST_NOT_ALLOWED`는 보안 설정(SSRF 방지)이 의도대로 동작한 결과다. 외부 CDN을 허용 목록에 넣을지는 보안 판단이 필요하다.
- `INVALID_URL`은 다운로드가 아니라 **base64 디코딩으로 처리할 문제**다. 네트워크를 타지 않으므로 보안 판단 없이 코드로 해결할 수 있다.

두 건 다 해결해도 호출량은 159 + 최대 29 = 188회로 상한 300 안에 들어온다.

### 처리 방법

| 원인 | 처리 |
| --- | --- |
| `INVALID_URL` 8건 | `decodeDataUrl()` 추가. 본문에 박힌 `data:image/…;base64,` 데이터를 파일로 만든다. 네트워크를 타지 않으므로 보안 판단과 무관하다. 판별 결과 JPEG 4건·PNG 4건으로 모두 실제 이미지였다 |
| `HOST_NOT_ALLOWED` 21건 | **이 배치에 한해** `blog.kakaocdn.net` 하나를 허용 목록에 더한다(`OCR_EXTRA_ALLOWED_HOSTS`). 전역 기본값(`www.geumjeong.go.kr`)은 그대로 두고, 와일드카드가 아닌 호스트 완전 일치이며 사설 IP 차단은 계속 적용된다 |

호스트를 넓히는 범위를 테스트로 고정했다. 목록이 정확히 한 개이고 와일드카드가 없어야 통과한다.

### 2차 결과 (2026-08-15, API 호출 0회)

| 항목 | 1차 | 2차 |
| --- | ---: | ---: |
| 대상 파일 | 244 | 244 |
| 체크섬 기준 고유 파일 | 159 | **188** |
| 내용이 같아 접힌 파일 | 56 | 56 |
| 이미지가 아닌 파일 | 0 | 0 |
| 다운로드 실패 | 29 | **0** |
| **예상 실제 호출** | 159 | **188회** |

상한 300회 안에 들어온다. 카카오 CDN 이미지 하나를 11개 게시글이 공유하는 경우가 있어 체크섬 접기가 계속 효과를 낸다.

## 9. 장식용 이미지 판별

프로그램 내용이 없는 장식·로고 이미지를 사람이 미리 골라낼 필요는 없다. **OCR 결과에 글자가 하나도 없으면 `OCR_NO_TEXT`로 자동 분류한다.**

- 판정 근거는 추출기가 이미 주는 `isEmpty`와 `fieldCount`다. 별도 규칙을 만들지 않는다.
- 신뢰도보다 이 판정이 우선한다. 신뢰도가 높아도 글자가 없으면 장식용이다.
- **폐기가 아니라 분류다.** 실제로는 글자가 있는데 OCR이 못 읽었을 수도 있으므로 목록에 남겨 사람이 확인한다. 근거가 약하면 자동 처리하지 않는다는 기존 원칙과 같다.

미리 골라내는 방식보다 나은 이유는 두 가지다. 첫째, 244개를 사람이 눈으로 분류하는 비용이 사라진다. 둘째, "글자가 없다"는 판단을 사람의 눈짐작이 아니라 실제 추출 결과로 하게 된다.

비용 측면에서는 장식용 이미지에도 호출이 한 번씩 나가지만, 예상 188회가 상한 300회 안이라 감당된다.

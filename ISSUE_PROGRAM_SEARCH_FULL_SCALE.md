# 정제한 351건으로 의미 검색 확장

## 배경

#165에서 37건(텍스트 정제 17건 + 첨부 정제 표본 20건)으로 의미 검색과 기획서 생성을 검증했다.
고정 질의 12개에서 `title+intro+target` 프로필이 Hit@1 91.7%, Hit@3 100%, MRR 0.958을 냈다.

#146에서 전체 351건 정제가 끝났다. 이제 같은 파이프라인을 351건으로 넓힌다.

**37건의 성적을 그대로 믿을 수 없다.** 37건 중에서 고르면 웬만하면 맞는다.
351건에는 서로 닮은 프로그램이 떼로 있어, 여기서 처음으로 변별력이 드러난다.

## 지금 상태

### 정제 결과 (#146 산출물)

| 항목 | 수치 |
| --- | ---: |
| 전체 프로그램 | 351건 |
| 회차를 확보한 프로그램 | 244건 (평균 7.2회차) |
| 총 회차 | 1,759개 |
| 소개·목표가 있는 프로그램 | 258건 |
| 본문만으로 게시 가능한 프로그램 | 227건 |
| 회차도 본문도 없는 프로그램 | 21건 |

경로별로는 `TEXT_WITH_IMAGE` 126건, `NO_TEXT_IMAGE_ONLY` 124건, `DOC_EXTRACT` 83건, `TEXT_ONLY` 18건이다.

### 입력 스키마는 이미 맞는다

파일럿 코퍼스 빌더(`buildProgramBoardSearchProfiles.ts`)는 DB가 아니라 로컬 JSON 두 개를 읽는다.

```
PROGRAM_BOARD_DATA_PATH         → .local/program-board/programs.json
PROGRAM_ATTACHMENT_SAMPLES_PATH → .local/program-attachment-enrichment/merged-samples.json
```

`merged-samples.json`(20건)과 `.local/program-attachment-batch/full.json`(351건)은 **같은 스키마**다.
full.json이 필드를 더 가질 뿐 20건짜리 키를 모두 포함한다.

```
merged-samples : schemaVersion, sourceId, title, basicInfo, board, curriculum, … (11개)
full.json      : 위 11개 + lane, bodyPublishable, sourceUrl, contentProfile, … (23개)
```

그러므로 **DB 작업 없이 확장할 수 있다.** 껍데기(`{ items: [...] }`)를 벗기는 어댑터면 된다.

## 이번에 풀어야 할 문제

### 1. 닮은 프로그램이 검색 결과를 잠식한다

제목에서 기수·차수·시간대 표기를 걷어내고 묶으면 **17개 묶음에 63건**이 몰린다.

| 건수 | 프로그램 | sourceId |
| ---: | --- | --- |
| 12 | 들락날락 영어랑 놀자 | 3595, 3596, 3702, 3703, 3849, 3850, … |
| 7 | 이야기로 만나는 동화나라 | 2952~2958 |
| 5 | 마술체험 | 2552~2556 |
| 5 | 그립톡 만들기 | 2557~2561 |
| 5 | 마스크 스트랩 만들기 | 2562~2566 |
| 5 | 다육이 행잉볼 만들기 | 2567~2571 |
| 3 | 향기로운 크리스마스 캔들 만들기 | 3779~3781 |

「영어 수업 찾아줘」 한 번에 영어랑 놀자 12건이 Top N을 다 잡아먹으면 검색이 쓸모없다.
반면 유아반과 초등반은 실제로 다른 프로그램이므로 무조건 하나로 접어서도 안 된다.

**어디까지 접고 어디부터 나눌지가 이번 이슈의 핵심 판단이다.**

### 2. 평가 질의 12개로는 351건을 재지 못한다

37건 기준으로 만든 질의다. 위의 닮은 묶음을 구분하는 질의가 없다.

### 3. 회차 1,759개를 검색에 어떻게 쓸 것인가

지금 프로필은 제목·소개·대상까지만 쓴다. 회차 내용은 검색 뒤 컨텍스트로만 붙는다.
「점토로 만들기 하는 수업」처럼 **회차에만 있는 말**로 찾는 질의는 지금 구조에서 안 걸린다.

회차를 검색 대상에 넣을지, 넣는다면 프로그램 단위 벡터에 합칠지 회차를 별도 문서로 쪼갤지 정해야 한다.
이 판단이 다음 이슈(DB 스키마)의 입력이 된다.

## 작업 범위

### 포함

- [ ] `full.json`을 코퍼스 입력으로 받는 어댑터
- [ ] 351건 검색 프로필·임베딩 재생성 (`program-board-search:profiles`, `:embeddings`)
- [ ] 닮은 프로그램 묶기 규칙 (기수·차수·시간대 변형 판정)
- [ ] 평가 질의 확대 (12개 → 30개 이상, 닮은 묶음 구분 질의 포함)
- [ ] 351건 기준 검색 성능 측정 및 37건과 비교
- [ ] 회차를 검색에 쓸지 실험하고 결론 내기
- [ ] 파일럿 UI(`/programs/search-pilot`)를 351건에 연결
- [ ] DB 스키마 요구사항 정리 (다음 이슈 입력)

### 제외

- DB 저장 및 pgvector 적재 (다음 이슈)
- 이용자용 게시판 UI·신청 버튼
- Gemini 기획서 생성 로직 변경 (#165에서 검증 완료, 그대로 쓴다)

## 판단이 필요한 것

### (1) 닮은 프로그램을 어떻게 다룰까

| 안 | 내용 | 장점 | 단점 |
| --- | --- | --- | --- |
| A | 대표 1건만 남기고 접기 | 결과가 깔끔 | 유아반·초등반 구분이 사라짐 |
| B | 전부 그대로 두기 | 원본 보존 | Top N을 한 프로그램이 독점 |
| C | 묶어서 보여주되 대상이 다르면 나누기 | 실제 의미에 맞음 | 규칙이 복잡 |

**C를 제안한다.** 정제 단계에서 세운 「원본을 최대한 보존한다」와 어긋나지 않으면서,
대상(유아/초등)이 다르면 실제로 다른 프로그램이기 때문이다.
다만 「마술체험 5건(같은 날 시간대만 다름)」처럼 대상까지 같은 건은 하나로 접어야 한다.

### (2) 회차를 검색 대상에 넣을까

| 안 | 내용 | 비고 |
| --- | --- | --- |
| A | 지금처럼 제목·소개·대상만 | 확장 비용 0, 회차 질의 못 잡음 |
| B | 회차 내용을 프로그램 벡터에 합침 | 싸지만 긴 문서가 희석됨 |
| C | 회차를 자식 문서로 쪼개 따로 검색 | 정확하지만 구조 변경 큼 |

**A로 먼저 재고, B를 실험한 뒤 C 필요 여부를 판단하자.**
C는 DB 스키마와 직결되므로 근거 없이 먼저 정하지 않는다.

### (3) 검색에서 뺄 건이 있을까

회차도 본문도 없는 21건은 검색 결과로 나와도 사용자에게 보여줄 내용이 없다.
빼는 게 맞는지, 아니면 제목만으로도 남길지 정해야 한다.

## 검증 방법

```bash
cd apps/backend
npm run program-board-search:profiles
npm run program-board-search:embeddings
npm run program-board-search:evaluate
npm run test:program-board-semantic-search
```

성공 기준

- 351건이 검색 코퍼스에 들어간다
- 확대한 평가 질의에서 Hit@3가 37건 대비 크게 떨어지지 않는다
- 닮은 묶음을 구분하는 질의에서 의도한 대상의 프로그램이 상위에 온다
- 한 프로그램의 변형이 Top 5를 독점하지 않는다

## 참고

- 정제 과정과 판단 근거: `PROGRAM_FULL_NORMALIZATION_PARTIAL_RESULTS.md`
- 파일럿 검색 결과: `PROGRAM_BOARD_SEMANTIC_SEARCH_PILOT_RESULTS.md`
- 파일럿 생성 결과: `PROGRAM_BOARD_DRAFT_EXPERIMENT_RESULTS.md`
- 임베딩은 `nlpai-lab/KURE-v1`을 로컬에서 생성하며 산출물은 커밋하지 않는다

## 관련 이슈

- 선행: #146 (전체 정제), #165 (검색·생성 파일럿)
- 후속: DB 저장 및 pgvector 적재, 이용자용 게시판 UI
